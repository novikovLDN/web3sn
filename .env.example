# Copy to .env for local dev. On Railway add these in Service → Variables.
# NOTE: Vite bakes VITE_* vars at BUILD time, so a redeploy is required after changing them.

# Email used by the "Contact Me" button (mailto:)
VITE_CONTACT_EMAIL=hello@jack3d.dev
