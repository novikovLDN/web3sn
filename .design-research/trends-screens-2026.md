# Тренды 2026–2027 — по экранам и по главной странице

Дополнение к `.design-research/trends-2026.md`. Тот отчёт остаётся базой по
позиционированию, копирайтингу, performance-craft, цвету и текстуре. Здесь —
то, чего в нём нет: **драматургия главной, приёмы «недоступности», подача
каждой из пяти дисциплин, кириллическая типографика и переходы**.

Дата сборки: июль 2026.
Стек проекта: React 18 + Vite + framer-motion 12 + Lenis 1.3.25 + three 0.169 / R3F 8 + Tailwind 3. **GSAP не установлен** — все рекомендации ниже даны так, чтобы работать на этом стеке.

---

## РАЗДЕЛ 0. Что в предыдущем отчёте устарело или спорно

Перед тем как что-то применять — поправки к `trends-2026.md`.

| Место | Что там сказано | Поправка |
|---|---|---|
| §1.1, список SOTD | Дан снимок Awwwards на июль 2026 | Список SOTD протухает за недели. Использовать как срез вкуса, не как «топ». Устойчивые референсы — не SOTD, а студийные сайты, которые живут годами (Locomotive, Obys, basement, Unseen). |
| §1.4 «Bento grid выстрелил» | +23% scroll depth, «стал дефолтом» | **Спорно и внутренне противоречит §5**, где bento назван «dated as style». Для портфолио senior-дизайнера bento — риск: это самая узнаваемая сетка AI-генерации. В `Modeling3DScreen` bento уже используется — это единственная слабая структурная точка того экрана (см. Раздел 2.3). |
| §1.4 «Dark mode по умолчанию» | +18% сессий | Цифра из одного маркетингового блога, непроверяема. При этом §6 того же отчёта честно фиксирует обратное: **все верифицированные топ-портфолио design-engineer'ов — светлые или переключаемые**. Противоречие не разрешено. Разрешение — в Разделе 1.4 ниже: не «тёмный сайт», а **температурная драматургия** (тёмный → светлый → тёмный). |
| §1.3 «whitespace +300% воспринимаемой ценности» | Подано как факт | Это цифра из блога Flux Academy без первоисточника. Принцип верный, цифра — не цитировать. |
| §2.2 прелоадеры | «~45% выборки» | Верно, но вывод важнее цифры: **на этом сайте прелоадер не оправдан**, кроме одного случая — если появится WebGL-hero, который реально грузит ассеты. Фейковый прелоадер прямо назван junior-tell'ом там же. |
| §5, последняя строка | AOS / parallax / mesh-gradient hero помечены «Unverified» | Оставить помеченными. Не строить на них аргументацию. |
| §1.7 / §4 (scroll-driven) | Автор сам пометил: «делегированное исследование не вернулось, перепроверить support» | Закрыто в Разделе 4 ниже. |
| Типографика §1.1–1.6 целиком | 60+ гарнитур | **Ни одного слова про кириллицу.** Это критический пробел: большая часть перечисленного (Apercu, GT America, Founders Grotesk, Canela, Ogg, Söhne, Editorial New, Migra, Reckless) кириллицы **не имеет** или имеет её только в дорогих расширенных пакетах. См. Раздел 5. |

---

## РАЗДЕЛ 1. Главная страница: «дорого и недоступно»

### 1.1 Механика недоступности

Базовый отчёт §2.6 уже дал главный вывод, и он контринтуитивный, поэтому его
надо повторить перед всем остальным:

> **Ни один сайт из выборки 24 топ-студий не использует датированную доступность.** Ни «currently booking Q3», ни «осталось 2 слота», ни счётчиков, ни зелёной точки «available for work». Ноль.

Причина: датированный дефицит **фальсифицируем и протухает**. Вернувшийся
посетитель, увидевший те же «limited slots» дважды, узнаёт, что его обманули.
**Премиальный дефицит структурен, а не темпорален.**

Эталон формулировки — Obys:

> `"Obys takes on a limited number of projects each year, partnering with marketing leaders and founders who value authorship, clarity and long-term brand impact"`

Четыре механизма в одной фразе: дефицит без числа и даты (нефальсифицируем);
`partnering with`, а не `working for` (симметрия); названа **сеньорность
покупателя** (отсекает тех, кто не подписывает); квалификация **по ценностям,
а не по бюджету** — сказано «если тебе нужно дёшево, это не сюда», ни разу не
сказав «дорого».

**Прямое следствие для этого сайта — три правки, каждая на одну строку:**

1. **Убрать плашку `Открыт для проектов` с зелёной пульсирующей точкой из hero.**
   Это ровно тот сигнал, которого нет ни у одной топ-студии. Базовый отчёт
   §2.6 формулирует выбор без компромисса: **«Соло-разработчик должен выбрать:
   прозрачность доступности (конвертит быстрее, цена ниже) ИЛИ избирательность
   (конвертит медленнее, цена выше). Оба сигнала одновременно невозможны.»**
   Заказчик просит «дорого и недоступно» — значит, плашка уходит. Токены
   `--ok / --ok-bright` после этого можно удалить из палитры: единственный
   цвет вне акцентной температуры исчезает, дисциплина становится абсолютной.
2. **Убрать `CONTACT.availableFrom`** (месяц доступности). Та же причина —
   это датированный дефицит, и `ИНСТРУКЦИЯ.md` уже честно предупреждает, что
   «устаревшая дата хуже отсутствующей». Правильное решение — не обновлять
   дату, а убрать поле.
3. **Добавить одну структурную строку об избирательности** — в `About` или в
   `FAQ`. По модели Obys, на русском, без числа и без даты. Например:
   «Беру ограниченное число проектов в год. Работаю с основателями и
   директорами по маркетингу, для которых важны авторство и длинный горизонт.»

**Уже сделанное правильно — не сломать.** Три вещи на сайте уже работают как
приёмы недоступности, и они сильные:

- **`Портфолио по запросу`** — «Большая часть работ закрыта соглашением о неразглашении и не публикуется». Это лучший из имеющихся сигналов: закрытое портфолио читается как признак уровня клиентов, а не как отсутствие работ. Надо только переставить его выше (Раздел 3-БИС).
- **`Кому вы отказываете`** в FAQ — прямое озвучивание отбора. Редкий и сильный ход. Ответ на этот вопрос должен быть самым конкретным на сайте.
- **Отсутствие цен** и отсутствие выдуманных отзывов (секции честно не рендерятся без данных).

### 1.1-БИС Приёмы недоступности: живые примеры, июль 2026

Отдельная проверка ~40 сайтов по паттернам `booked`, `waitlist`, `referral`,
`per quarter`, `no longer accepting`, `2027` дала **почти ноль совпадений**.
Это независимо подтверждает вывод базового отчёта §2.6: датированной
доступности на верхнем рынке нет. Дороговизна кодируется иначе — вот чем.

**1. Квалификация бюджетом внутри диалогового intake — золотой стандарт.**
[Locomotive](https://locomotive.ca/en/contact) заменил форму на **L.I.S.A.**
(Locomotive's Interactive Super Assistant) — ветвящийся видео-диалог. Четыре
входные ветки: `Start a project` / `Join the team` / `Drop a quick word` /
`Discover our culture`. Шаг `project-budget` **обязателен и не пропускается**,
вилки зависят от типа проекта: branding `25k–50k / 50k–75k / 75k–100k / 100k+`,
digital `75k–125k / 125k–200k / 200k+`. Вопрос о бюджете написан в пяти
рандомизируемых вариантах, verbatim:

> `"We've reached the part where dreams meet dollars. What budget are we working with?"`
> `"I need a number. You need a great project. Let's help each other. What's your budget range?"`

Плюс отдельный публичный блок **«The ideal digital brief»** — нумерованный
список из 8 требований **к клиенту** (Project Overview, Digital Requirements,
Content Strategy, Brand and Design, Technical Specifications, Content
Management, Budget Constraints, Schedule and Milestones).
*Почему дорого:* нижняя граница входа (25k) объявлена до того, как вы написали
хоть слово. Это отсев, замаскированный под обаяние. А чек-лист брифа буквально
переворачивает роли — клиент готовится к вам.

**2. Гейт до контента.** [Robin Payot](https://robinpayot.com/) —
`Enter` / `Scroll to explore` / **`This website has been designed for desktop`** /
`Please rotate your device`. Мобильному посетителю прямо сообщают, что сайт не
для него. *Почему дорого:* инверсия сервисной логики — не сайт адаптируется под
вас, а вы под сайт. У [Resn](https://resn.co.nz/) в коде лежит отдельный ассет
`img/old-browser.png` — графика «устаревший браузер» того же класса.

**3. Аудиогейт, где дефолт — со звуком.** [Unseen Studio](https://unseen.co/) —
кнопка называется **`Enter without audio`**, то есть отказ от звука подан как
урезанная версия. Лоадер: `background:#212121`, `color:#424242` — почти
нечитаемый контраст как намеренный приём.
[Exo Ape](https://exoape.com/) — мягкая версия: `"Our work is best experienced
in motion. Don't forget to put on your headphones."` Это инструкция, а не просьба.

**4. Ноль CTA на первом экране — высшая форма отбора.**
[Antinomy Studio](https://antinomy.studio/) — весь первый экран это одна строка:
`"Antinomy is an independent creative studio shaping contemporary brands."`
Ни кнопки, ни «Let's talk». [Izanami](https://izanami-official.com/) (SOTD,
18.07.2026) — первый экран это фраза `"Remember who you are"`, первая кнопка —
`View Philosophy`, не «Contact». [Obys](https://obys.agency/) — контактной
страницы вообще нет (`/contact` отдаёт 404), только строка в футере.
*Почему дорого:* отсутствие продажи = отсутствие нужды продавать.

**5. Условная доступность вместо календаря.**
[Henry Desroches](https://henry.codes/) — блок `Availability`, verbatim:
> `"I'm still taking on freelance projects on a case-by-case basis."`

**`case-by-case basis` — самая точная найденная замена «2 клиента в квартал»:**
не называет число, но однозначно сообщает, что решение принимает исполнитель.
Плюс отдельная страница **`Now`** («Current focus, soaring dreams and steady
sightlines») — делает статус динамическим, ничего не обещая.

**6. Платная консультация как единственный быстрый канал.**
[Emil Kowalski](https://emilkowal.ski/): `"Enrollment has closed"` +
`"You can also book a paid call with me if you need advice or have specific
questions."` *Почему дорого:* назначает цену вниманию, не декларируя статус.
Вероятно, самый эффективный механизм недоступности из найденных.

**7. Форма, где поле бюджета набрано кеглем заголовка.**
[basement.studio/contact](https://basement.studio/contact) — три поля
(`name`, `budget`, `message`), плейсхолдер буквально `Budget (optional)`, класс
поля `xl:h-[56px] xl:text-[56px] xl:tracking-[-2.24px]`. **Ввод набирается
56 пикселями с трекингом −2.24px** — поле визуально равно H1. Кнопка —
`Submit Message`, без заигрываний. *Почему дорого:* масштаб типографики
говорит «этот ответ важен», а `(optional)` рядом с бюджетом — приём статуса.

**8. Два раздельных адреса.** [Lusion](https://lusion.co/):
`General enquires — hello@lusion.co` / `New business — business@lusion.co`.
[Immersive Garden](https://www.immersive-g.com/) идёт дальше — **только**
`inquiries@immersive-g.com`, никакой формы. *Почему дорого:* отсутствие формы
= отсутствие воронки.

**Антипримеры среди сильных студий — чего не делать:**
[Dennis Snellenberg](https://www.dennissnellenberg.com/) публикует **телефон
`+31 6 27 84 74 30` прямо на главной** — сильнейший сигнал доступности, то
есть противоположность задаче. [Lusion](https://lusion.co/) технически один из
лучших сайтов в мире, а копирайт — `"Is Your Big Idea Ready to Go Wild?"`,
`"Let's work together!"`, футер `"Built by Lusion with ❤️"`.
[Dogstudio](https://www.dogstudio.co/contact) — рандомайзер из 34 шуток
(`"If you like burritos, drop us a line"`, `"If you're a loser, drop us a line"`);
обаятельно в 2018, в 2026 читается как «мы возьмём любого».

**Что стоит добавить из арсенала, которого пока нет:**

- **Квалификация на форме.** Базовый отчёт §2.6 разбирает механику Cuberto: поле «бюджет проекта» с фиксированными вилками, где минимальная опция — уже высокая. «Сообщение — это пол: все с бюджетом $3k самоотсеиваются до контакта. Форма делает квалификацию, чтобы студии никогда не пришлось говорить "мы дорогие"». Сейчас форма в `ContactSection` — три поля (Имя / Почта / Задача) без квалификации. Добавление вилки бюджета делает работу прайс-страницы без прайс-страницы.
- **Два адреса почты** (общий и по новым проектам) — дешёвый сигнал, что входящих достаточно, чтобы их сортировать.
- **Реквизиты юрлица** в футере. Базовый отчёт §2.8: «Для соло-разработчика это непропорционально мощно: ИНН/номер говорит "я могу выставить счёт вашему отделу закупок", а это и есть реальный блокер на крупных сделках».
- **Год начала практики** — по §2.6 доминирующий долговечный сигнал авторитета: дёшев, вечен, нефальсифицируем. Работает лучше, чем «8 лет опыта», потому что не требует обновления.

**Вербально: минус превосходные степени, плюс преуменьшение.** Тон в
`content.ts` («Сдержанная авторитетность. Дорогой специалист не убеждает —
он излагает») задан верно и совпадает с выводом §2.7. Один приём, которого
пока нет — **самоирония**: «Только тот, кому не нужна работа, может шутить
про работу». Одна такая строка на всю страницу делает больше, чем абзац
о компетенциях.

### 1.2 Первый экран: погружение вместо статичного заголовка

**Что есть сейчас.** `ReactiveGrid` — 2D-канвас, сетка точек `SPACING 46px`,
влияние курсора `RADIUS 190px`, простаивающая синусоида `t += 0.004`, точки у
курсора окрашиваются в акцент. DPR ограничен до 2, rAF-цикл останавливается
`IntersectionObserver`'ом при уходе hero из кадра, `prefers-reduced-motion`
убивает волну.

Это сделано **технически безупречно** и стоит почти ноль. Но как приём это
самый распространённый «интерактивный фон» в вебе — точечная сетка, реагирующая
на курсор, встречается в шаблонах Framer и Webflow сотнями. Заказчик просит
погружения; сетка точек его не даёт.

**Хореография загрузки** сейчас: nav `0.1` → eyebrow `0.25` → `NOVIKOV`
посимвольно `0.35` (`SplitText`, маска из `y: 110%`, `duration.slower 0.95`,
`stagger.letter 0.018`, суммарно ≤0.6s) → statement `0.7` → плашка и кнопки
`0.85` → нижняя панель `1.1`, дисциплины `1.2 + i*0.09`. Итого ~2 секунды
выстроенного входа. **Это хорошо и это надо сохранить** — менять стоит не
тайминг, а то, во что этот тайминг разрешается.

**Что реально стоит на первых экранах в июле 2026** (стеки подтверждены
прямыми маркерами в HTML/JS и тегами Awwwards «Technologies & Tools»):

| Сайт | Стек | Что происходит |
|---|---|---|
| [Izanami](https://izanami-official.com/) — SOTD 18.07.2026 | WebGL + GSAP + Lenis | Одна фраза `"Remember who you are"`. **WebGL обслуживает тишину, а не аттракцион** — ритуальный медленный сайт, а не демо возможностей. |
| [Hiroto Sato](https://hirotos.com/) — SOTD 17.07.2026 | WebGL + GSAP | **Прямой референс задачи: соло-разработчик, взявший SOTD.** Статический HTML практически пуст — вся страница строится в рантайме, полностью canvas-driven. |
| [darkroom.engineering](https://darkroom.engineering/) (бывш. Studio Freight) | three.js + r3f + ogl + Lenis + GSAP + Rive + video | Первый экран — `"WhereThingsGetDeveloped"` **слитно, без пробелов**: типографика как знак, а не как текст. Они же авторы Lenis. |
| [Robin Payot](https://robinpayot.com/) | WebGL + canvas | Гейт `Enter` → сцена. Прелоадер нужен, потому что сцена тяжёлая — и его превратили в ритуал входа. |
| [Unseen Studio](https://unseen.co/) | WebGL + canvas + audio | Вертикальная анимированная типографика + аудиогейт. Пара **Neue Montreal + Saol Display Light Italic**. |
| [Hello Monday](https://www.hellomonday.com/) | r3f + PixiJS + GSAP | Pixi (2D-шейдеры) и r3f (3D) в одном проекте — редкая и дорогая комбинация. |
| [Vectr](https://vectrfl.com/) — SOTD 11.07.2026 | **Rive** + preload | Rive вместо Lottie — заметный сдвиг 2026: Rive реагирует на состояние, Lottie только проигрывается. |
| [Phantom](https://phantom.land/) | WebGL + AR | В описании кейса: «responsive orb visualiser and **restrained** graphic language». Сдержанность декларируется как ценность прямо в описании 3D-работы. |

**Типографика, разобранная на части.**
[14islands](https://14islands.com/) разбирает навигацию и заголовок **на
отдельные буквы** прямо в DOM (`W o r k`, `S e r v i c e s`) — прямая улика
per-character реакции на курсор. [Antinomy](https://antinomy.studio/) и
[Immersive Garden](https://www.immersive-g.com/) разбивают **пословно**.
**Практический вывод: пословный reveal читается как речь, посимвольный — как
эффект.** Сейчас в `HeroSection` стоит `SplitText by="char"` на `NOVIKOV` —
для одного слова-логотипа это правильно, но для строки-высказывания под ним
нужен `by="word"`.

**Прелоадер как элемент контроля.** У [Locomotive](https://locomotive.ca/en) в
CSS: `html.is-loading { cursor: wait }` и `.is-first-loading body { overflow: hidden }`.
**Сайт явно меняет курсор на «ждите»** — микросигнал того, кто здесь
распоряжается временем.

**Три направления, по убыванию отношения эффект/риск:**

**(а) Заменить сетку точек на одну вещь с весом.** Базовый отчёт §4 даёт
формулу выжившей премиальной формы: **«одна твёрдая идея, выполненная чисто —
один объект с настоящим весом, атмосферой и кадрированием, а не нагромождение
эффектов»**, и там же — предупреждение, что тяжёлый WebGL-hero по умолчанию
является обязательством, а не активом (800kB–2MB JS до первого пикселя).

Разрешение противоречия для этого проекта: **three.js уже в бандле** (экраны
услуг и 3D-остров), значит маржинальная стоимость hero-сцены — не рантайм, а
только сама сцена. Условия, при которых это оправдано:
- сцена грузится **после** первого пикселя, поверх уже отрисованного статичного кадра;
- есть честный фолбэк (механика `shaderFailed` из `MotionScreen` уже написана);
- сцена — один объект, а не система частиц.

**(б) Довести существующий канвас, не заменяя его.** Дешёвый путь с высоким
эффектом: сетка точек остаётся, но начинает реагировать не только на позицию
курсора, но и на **скорость** его движения и на скорость скролла. В
`MotionScreen` это уже реализовано — `useVelocity(scrollY)` →
`useTransform([-2500,0,2500],[-5,0,5],{clamp:true})`. Поле, которое
«запаздывает» и «догоняет», ощущается как материя; поле, которое просто
подсвечивается под курсором, ощущается как эффект.

**(в) Заголовок, который реагирует на указатель.** `NOVIKOV` набран `MTS Wide` —
переменного начертания у шрифта нет, поэтому вариативная ось отпадает. Но
доступен трекинг: расстояние между литерами слегка расходится под курсором и
собирается обратно. Пружина `spring.gentle {180, 26, 1}`. Приём тихий,
работает на любом шрифте и не трогает layout, если применять к
`letter-spacing` на отдельных `<span>` с зафиксированной шириной контейнера.

**Про звук.** Аудиогейт на входе (`Enter` / `Enter without audio`, как у Unseen
Studio из §2.2 базового отчёта) — самый сильный приём «церемонии» из
существующих. Но он оправдан **только** вместе с (а): звук на сайте без
тяжёлой сцены читается как претензия. Если hero остаётся лёгким — звука быть
не должно.

**Про прелоадер.** `LoadingScreen` уже есть, и он сделан правильно — прогресс
по реальным сигналам (`document.fonts.ready` = 45, `window.load` = 55), пол
900ms, потолок 1900ms, выход `y: '-100%'` за `duration.slower` с
`ease.editorial`. Базовый отчёт §2.2 предупреждает: «Фейковый прелоадер на
лёгком сайте — junior-tell, издержка без актива за ней». Здесь прогресс
честный, так что претензии нет. Но **пол в 900ms — это уже удержание**:
если ассеты пришли за 300ms, сайт держит посетителя лишние 600ms. Оправдать
это можно ровно одним способом — сделать то, что делает Locomotive: превратить
время загрузки в **высказывание**, которое собирается по фразе. Тогда 900ms
— это не задержка, а предложение, которое нельзя пропустить.
Чего категорически не делать — цикла приветствий `Hello / Bonjour / Guten tag`:
по §2.2 это самый скопированный прелоадер в вебе, и в 2026 он идентифицирует
автора как последователя.

### 1.3 Эмоциональная драматургия длинной страницы

Здесь основная работа, и здесь же самый большой разрыв между текущим сайтом и
задачей. Сейчас страница ритмически **ровная**: после hero каждая секция —
типографика плюс волосяные линии, на одном фоне `--surface`, с одинаковым
вертикальным отступом `--section-y: clamp(5rem, 12vh, 11rem)` и одинаковым
паттерном появления (`Reveal`, `inView { once: true, amount: 0.25 }`).
Дисциплинированно — и монотонно. Единственный температурный сдвиг за всю
страницу — светлая `ServicesSection` (`--n-950`), и он потрачен на самую
скучную секцию.

**Четыре инструмента ритма, в порядке силы:**

**(1) Температура фона как драматургия, а не как разделитель.**
Правило: **одна инверсия на страницу, на самой сильной секции**. Сейчас
инверсия стоит на `Services`. Переставить её на `Projects` — на «Портфолио по
запросу». Тогда самое сильное высказывание сайта («работы закрыты NDA»)
получает единственный за всю страницу удар светом. Всё остальное остаётся
тёмным, и от этого удар работает.

Техника — анимировать **только** `background-color`, по базовому отчёту §6:
«Scroll-driven inversion на границах секций, анимируется только
`background-color`. Дёшево, эффектно, не требует WebGL». Никакого
`backdrop-filter`, никаких смешений.

**(2) Полноэкранные паузы.** Между плотными секциями — экран, на котором почти
ничего нет: одна строка, много воздуха, фон `--temp-deep #060505` (глубже
базового). Пауза не несёт информации; она нужна, чтобы следующая секция
читалась как удар. Три подряд идущие секции одного такта (Process →
Testimonials → FAQ) — это ровно место, куда просится пауза.

Базовый отчёт §1.3 даёт норму воздуха: контент занимает 45–60% ширины
вьюпорта, вертикальные отступы между секциями 12–20vh, не 80px. Текущий
`--section-y` максимум `11rem` (176px) — это **ниже нормы** на больших
экранах. Для секций-пауз нужно `min-height: 100svh`, а не отступ.

**(3) Контраст плотности, а не только размера.** Дорогая длинная страница
чередует: плотный информационный блок → почти пустой экран → плотный.
Сейчас плотность на главной ровная от Hero до Contact. Дешёвый способ создать
контраст — не добавлять, а **вычитать**: убрать `StatsSection` как секцию и
вернуть её одной строкой прозы (Раздел 3-БИС), убрать `TestimonialsSection`
целиком.

**(4) Дыхание вместо однократного появления.** `inView { once: true }` глобально
означает, что страница «включается» один раз и дальше стоит статично. Для
одной-двух ключевых секций стоит завести `{ once: false, amount: 0.4 }` —
секция слегка гаснет, уходя из кадра, и оживает, возвращаясь. Применять
**точечно**: если так ведёт себя вся страница, это читается как AOS-скрипт
(и базовый отчёт §5 честно помечает AOS-подобные раскрытия как
неподтверждённый, но вероятный признак дешевизны).

**Предлагаемая партитура главной:**

```
Hero                 тёмный, движение, ~2s хореография входа        [удар]
Marquee              тёмный, одна строка, быстро                    [выдох]
Projects / NDA       СВЕТЛЫЙ #f7f4ec — единственная инверсия        [удар]
пауза                #060505, одна строка, 100svh                   [тишина]
About                тёмный, портрет, длинный текст                 [плотно]
Services             тёмный, пять строк, интерактив                 [плотно]
Process              тёмный, рельс на скролле                       [средне]
пауза                #060505, 100svh                                [тишина]
Stats-в-строку       тёмный, одна строка прозы                      [быстро]
FAQ                  тёмный, две колонки                            [плотно]
Contact              тёмный, огромная почта, полный вьюпорт         [удар]
```

Базовый отчёт §2.9, дефолт №7: «Футер = полный вьюпорт, огромный email,
локальное время, copy-to-clipboard». Локальное время — сильная деталь: она
тихо отвечает на вопрос часового пояса и одновременно показывает, что сайт
живой.

### 1.3-БИС Три разобранные партитуры

*(реконструкция по разметке и ассетам, не визуальное наблюдение — на heavy-WebGL
сайтах JS не исполнялся)*

**[Izanami](https://izanami-official.com/) — ритм «медленного дыхания».**
Наиболее релевантный образец для задачи.
1. Тишина: `"Remember who you are"`, WebGL-фон, ноль CTA.
2. Философия: `"Sharing the Japanese Spirit of Harmony"`.
3. Объявление структуры: `"Through three practices, Izanami designs harmony across life."`
4. **Три равных блока-удара**: School / Craft / Retreat.
5. Пауза-манифест: самый длинный текст на странице.
6. Затухание: футер с двумя офисами и двумя часовыми поясами.

**Ключевой приём: три направления поданы как три равные практики, а не как
«кейс 1, 2, 3». Ни одно не выделено как флагман.** Это структурный отказ от
иерархии продаж — и прямая модель для пяти дисциплин в `ServicesSection`.

**[Lusion](https://lusion.co/) — ритм «нарастающей громкости», и почему он
ломается.** Hero → манифест `"Bold Ideas, Brought to Life"` → **пауза-переключение
модальности** (`Our Approach` / `Play Reel`) → Featured Work → философский блок
(`"We do not chase trends"`) → кульминация → и **провал финала**:
`"Is Your Big Idea Ready to Go Wild?"` → `"Let's work together!"` →
`"Built by Lusion with ❤️"`.

**Урок прямой: безупречно выстроенное нарастание уничтожается последним
экраном. Финал длинной страницы — самая дорогая недвижимость на сайте, и
эмодзи-сердце там стоит дороже всего остального вместе взятого.**

**[Henry Codes](https://henry.codes/) — ритм «личного письма».**
Провокация вместо оффера → идентификация → ударный разворот → **инструкция
читателю** (`"(Scroll with caution and with care.)"`) → **живые данные вместо
саморекламы** (`48ºF @ N 39º 43' 31.56" W 104º 58' 0.94" (Denver, Colorado) 6:44 PM`)
→ навигация как оглавление книги (`Garden`: Articles / Notes / Case Studies;
`Meta`: About / Now) → **полная остановка — письмо** (`"a brief letter from the
editor / dear reader (and in fact, dear friend)"`) → тишина → Availability →
Selected works, где имена клиентов (Stripe, YouTube, The New York Times)
**повторены по 5–6 раз подряд с намеренными переносами** (`YouT ube`,
`The Ne w York Times`) — бегущая строка, где имя клиента становится текстурой.

Шрифтовая пара: PP Neue Montreal Book + Louize Display Italic + Manuka Black.
**Три гарнитуры разных регистров** — гротеск, литературный курсивный сериф,
тяжёлый дисплейный. Именно контраст регистров создаёт температурные перепады
между секциями, без единой смены фона.

### 1.4 Что в 2026 читается как устаревшее и дешёвое

Сводная таблица «что умерло → чем заменено», с примерами.

| Устарело | Чем заменено | Пример замены |
|---|---|---|
| **Тикающие счётчики достижений** («90+ проектов», «8 лет опыта») | **Живые данные**: координаты, локальное время, погода, версия сайта. Счётчик достижений — утверждение о себе; координаты — факт о мире. Первое требует доверия, второе нет. | [henry.codes](https://henry.codes/), Obys (`CET 00:00 AM`) |
| **Отзывы с аватарками** | Пресса, награды, имена брендов как факт. Цитата клиента — просьба поверить; имя Cartier рядом с кейсом — факт. | [exoape.com](https://exoape.com/) (блок `In the media`), [Immersive Garden](https://www.immersive-g.com/) |
| **`Let's work together` + эмодзи в футере** | Отсутствие CTA + один e-mail. Призыв к совместной работе предполагает, что вы свободны. | [antinomy.studio](https://antinomy.studio/), [obys.agency](https://obys.agency/) |
| **Телефон на главной** | `case-by-case basis` + только почта | [henry.codes](https://henry.codes/) |
| **Marquee из SVG-логотипов** | Текстовая бегущая строка с намеренными переносами; либо собственное имя вместо чужих логотипов (`WE ARE OCHI WE ARE OCHI`) | [henry.codes](https://henry.codes/), [ochi.design](https://ochi.design/) |
| **Inter / бесплатные Google-гротески как единственный шрифт** | Лицензионная триада: гротеск + дисплейный сериф + mono для метаданных. **У трёх студий из выборки моно именно для техданных — это новая норма** для тегов, координат, времени, версий. | [darkroom.engineering](https://darkroom.engineering/) (Replica LL + Replica Mono LL), [henry.codes](https://henry.codes/) |
| **Стоковые шрифты вообще** | Собственная гарнитура студии (`ObysSans4.woff2`) | [obys.agency](https://obys.agency/) |
| **Scroll-hijack и spring-анимации везде** | Lenis — нормализовать инерцию, а не отбирать управление | [darkroom.engineering](https://darkroom.engineering/) — они же авторы Lenis |
| **Lottie** | **Rive** — реагирует на состояние, а не просто проигрывается | [vectrfl.com](https://vectrfl.com/) |
| **Структура лендинга-воронки** `Hero → Services → Process → Pricing → FAQ → CTA` | Структура **издания или архива**. Ни один сайт выборки не использует воронку. | Izanami (`philosophy / projects / school / craft / retreat`), Henry Codes (`Garden / Meta`), Obys (только `Work / About`) |
| **Открытая контактная форма** | Квалифицирующий intake с вилками бюджета | [locomotive.ca/en/contact](https://locomotive.ca/en/contact) |
| **Демонстрация 3D ради демонстрации** | «restrained graphic language» — 3D как атмосфера | [phantom.land](https://phantom.land/) |
| **Шуточный копирайт на контакте** | Юмор, **обслуживающий квалификацию** («We've reached the part where dreams meet dollars»), а не заменяющий её | Locomotive против [Dogstudio](https://www.dogstudio.co/contact) |
| **«prose-poem кейс»** — hero + два предложения прозы + credits + «visit live site» | Живое интерактивное доказательство внутри кейса | см. Раздел 2.5 |

**Универсальный антипаттерн 2026, независимо подтверждённый по всем пяти
дисциплинам:** любой эффект, покупаемый как GSAP / Lenis / ReactBits
boilerplate — кастомный курсор, magnetic button, grain overlay, fade-up-stagger,
фейковый прелоадер — **теперь читается как отсутствие авторского голоса, а не
как мастерство**. Это прямо касается проекта: `CustomCursor`, `Magnetic`,
`.grain` и `Reveal` — все четыре есть. Каждый из них должен либо делать что-то
неочевидное, либо быть убран.

**Отдельная заметка про структуру сайта.** Текущая главная — это буквально
`Hero → About → Stats → Services → Projects → Process → Testimonials → FAQ → Contact`,
то есть каноническая структура лендинга-воронки, которую **ни один сайт из
проверенной выборки не использует**. Перестройка порядка (Раздел 3-БИС) — не
косметика, а смена жанра: с воронки на архив.

---

## РАЗДЕЛ 2. По экранам

**Общий принцип, выведенный из двух эталонных экранов** (обоснование — Раздел 3.3):
экран читается дорого не когда на нём много эффектов, а когда на нём есть
**четыре вещи одновременно**:

1. одно персистентное состояние, которым управляет посетитель, и оно расходится по всему экрану;
2. одно число, посчитанное на странице, а не заявленное словами;
3. собственная типографическая шкала экрана (не только `var(--t-*)`);
4. одна аффорданса, объявленная посетителю прямым текстом.

`WebDesignScreen` и `BrandingScreen` имеют все четыре. `MotionScreen` имеет
полторы, `DevelopmentScreen` — ноль, `Modeling3DScreen` — ноль.

---

### 2.1 Motion-дизайн — `MotionScreen.tsx`

**Текущее состояние.** Технически самый богатый экран: R3F Canvas с
собственным GLSL (`MotionShader.tsx`, uniforms `uTime/uPointer/uPointerDir`,
simplex-шум), детект WebGL с фолбэком `shaderFailed`, кастомный `CursorRing`
поверх канваса (`cursor: none`), `Magnetic` кнопка, sticky-сцена на 280vh,
skew на скролл-скорости через `useVelocity(scrollY)` →
`useTransform([-2500,0,2500],[-5,0,5],{clamp:true})`, `DopeSheet` как навигация.
Палитра «петроль» — `#071316 / #0E2226 / #1F5C63 / #7FB2AE / #E2725B`,
Bricolage Grotesque + Geist Mono.

**Диагноз.** Экран показывает, что автор **умеет** моушн, но не показывает,
что автор его **понимает**. `EasingLab` и `TimingScale` локально интерактивны,
но ничего из того, что посетитель там настроил, не влияет на остальной экран.
Плюс сам файл — 389 строк, и секции 3/6/9/10 (statement, три принципа, большое
слово, CTA) — это статические текстовые блоки. Против пяти полностью
интерактивных секций у `WebDesignScreen`.

**Три приёма, которые стоит внедрить:**

**(а) Глобальная кривая экрана.** Сделать `EasingLab` источником правды:
выбранная посетителем кривая становится CSS-переменной на корне экрана, и на
неё переезжают **все** переходы экрана — появления секций, hover-состояния,
sticky-сцена. Это прямой аналог дока палитры в `BrandingScreen`.

```tsx
// значение из EasingLab пишем в корень экрана
<div style={{ '--screen-ease': `cubic-bezier(${c.join(',')})` } as CSSProperties}>
```

Почему читается дорого: посетитель не смотрит на демо кривых — он **живёт**
внутри выбранной кривой. Разница между «вот график easing» и «весь интерфейс
теперь двигается так» — это разница между иллюстрацией и авторством.

**(б) Живое число.** Сейчас `TimingScale` пересказывает `duration.*`, но
ничего не вычисляет. Базовый отчёт §3c даёт готовый закон, который можно
показать живьём: **постоянная воспринимаемая скорость**, где длительность —
функция пройденного расстояния (<100px → 200ms, 100–500 → 300ms, 500–1000 → 400ms,
полный вьюпорт → 500–600ms). Слайдер расстояния → на экране считается
рекомендованная длительность и тут же проигрывается на реальном объекте.
Это ровно то, что делает `WebDesignScreen` с трекингом и интерлиньяжем.

**(в) Разбор до/после.** Одна секция, где один и тот же переход показан дважды:
слева — с линейной кривой и без стаггера, справа — с `ease.entrance` и
`stagger.item 0.09`. Синхронный запуск по одной кнопке. Компетенция моушн-дизайнера
доказывается **сравнением**, а не демонстрацией; показать плохой вариант рядом
может только тот, кто уверен в хорошем. Механика уже написана в
`WebDesignScreen` (drag-шторка на компенсированном `scaleX`) — переиспользуется.

**Что делают сильные моушн-сайты в 2025–2026 — с источниками:**

- **Scroll-driven canvas frame-sequence вместо `<video>`.** Рендер в последовательность WebP, отрисовка через `drawImage` в canvas под DPR, **directional preloading** (подгрузка вперёд/назад по направлению скролла), урезанный набор кадров на мобильных. Убирает stutter при быстром свайпе — то, что арт-директор замечает мгновенно. Разбор перехода: [Codrops / OPTIKKA, окт 2025](https://tympanus.net/codrops/2025/10/16/creating-smooth-scroll-synchronized-animation-for-optikka-from-html5-video-to-frame-sequences/). Базовый отчёт §11.3 называет это «техникой Apple».
- **WebCodecs / `VideoDecoder` для настоящего покадрового скраба** — там, где `video.currentTime` ненадёжен (Firefox). Обёртка *mediabunny* проще, чем mp4box; на сервере видео переэнкодится с укороченным `-g` и `tune fastdecode`. [Codrops / KAI Design Dept., ноя 2025](https://tympanus.net/codrops/2025/11/20/behind-the-kai-design-dept-experience-webgl-line-blur-video-scrubbing-and-3d-animation/).
- **Лоадер, перетекающий в героя.** Визуалы лоадера становятся первыми слайдами; между кейсами — continuous transition вместо хард-катов. Портфолио перестаёт быть каталогом и становится смонтированным фильмом. [Codrops / Bisous, июнь 2026](https://tympanus.net/codrops/2026/06/29/inside-bisous-designing-an-editorial-experience-for-cinematic-cgi/).
- **Named easing прямо в тексте кейса** — `power4.out` на вход, `power3.out` на вторичные движения, с объяснением, почему именно эта кривая. Для арт-директора, который сам сидел в After Effects, это язык профессионала. **На этом проекте это бесплатно: система в `motion.ts` уже именована.**
- **Кинетическая типографика с намеренной неидеальностью** — буквы летят по трёхточечной кривой Безье, stagger ~0.025s, на 65% прогресса кросс-фейд с итоговым текстом. [Разбор двух портфолио, Codrops, дек 2025](https://tympanus.net/codrops/2025/12/02/two-portfolios-one-process-where-design-motion-and-code-come-together/).

**Чего избегать:** full-bleed autoplay Vimeo в hero без связи со скроллом
(признание, что сам сайт не является демонстрацией); фейкового процента
загрузки, не привязанного к реальным ассетам; курсора-точки, который просто
увеличивается на hover; дефолтного GSAP fade-up-stagger на каждом текстовом
блоке; бесконечной ленты логотипов.

---

### 2.2 Разработка — `DevelopmentScreen.tsx`

**Текущее состояние.** Терминал с печатью (16ms/символ, пауза 1400ms),
`BrowserWindow` с превью, `CodeMarquee` **дважды**, шесть sticky-стопкой
карточек этапов (`scale = 1 - (total-1-i)*0.03`), кольцо технологий на 320vh
со скролл-вращением (`angle = useTransform([0,1],[0,TAU])`), фоновые плавающие
глифы `</> { } ( ) ;` на `opacity 0.1`. Палитра «зелёный терминал» —
`#04070a / #0b1117 / #4ade80`, Handjet + JetBrains Mono.

**Диагноз — самый серьёзный из пяти.** Экран показывает **эстетику**
разработки, а не инженерную культуру. Разница принципиальна, и базовый отчёт
§2.8 её формулирует жёстко: «Дизайнерское портфолио оценивается по артефакту;
разработческое — по свидетельствам суждения… любой может заказать красивое
портфолио». Зелёный терминал, плавающие скобки и бегущая строка кода — это
**визуальные цитаты** разработки, то есть ровно то, что заказывают, когда
разработки нет.

Конкретно:
- Ноль собственной типографики: всё через `var(--t-h1)` / `var(--tr-h1)`, ни одного `clamp()`, ни одного решения про интерлиньяж. На фоне специмена Archivo/IBM Plex Mono в `WebDesignScreen` читается как заготовка.
- `STANDARDS` — это проза-утверждения («60 fps», «контраст»), не подтверждённые на странице ничем. Это ровно тот провал, которого избегает `BrandingScreen`, считая WCAG живьём.
- Магические числа вне системы: `speed = 16`ms и пауза `1400` захардкожены (должно быть `duration.cinematic * 1000`), шаг `0.03` в стопке карточек не объяснён.
- Декоративный фон вместо доказательного. У эталонных экранов нет украшений: каждый слой что-то доказывает.
- Две одинаковые `CodeMarquee`.

**Три приёма:**

**(а) Заменить `STANDARDS` на измеренную панель.** Не «60 fps» словами, а живой
счётчик кадров, число draw-call'ов сцены и вес бандла экрана, посчитанные на
странице в момент просмотра. Базовый отчёт §10 даёт готовый инструментарий:
`Long Animation Frames (LoAF)` API и `web-vitals` с attribution-сборкой.
Почему дорого: заявление «я забочусь о производительности» стоит ноль;
счётчик, который посетитель может испортить, открыв тяжёлую вкладку —
это проверяемое утверждение. Плюс он ловит инженерную аудиторию мгновенно.

**(б) Назвать один компромисс.** Базовый отчёт §2.8, сигнал senior №2:
«Названные ограничения и компромиссы, а не названные инструменты… Списки
инструментов ранжируют тебя среди других джунов; заявленные компромиссы
вынимают тебя из сравнения». На экране должен появиться блок формата
«здесь я выбрал X вместо Y, и вот чем заплатил». Для этого сайта есть
готовый честный пример: **Lenis вместо нативного скролла** — куплена
инерция, заплачено конфликтом с `position: sticky`, доступностью и
`scroll-driven animations`; отсюда отключение при `pointer: coarse` и
`prefers-reduced-motion`. Это настоящее решение с настоящей ценой.

**(в) Собственная типографическая шкала + убрать декор.** Явные `clamp()`
с решениями про трекинг и интерлиньяж (с учётом кириллической поправки
`--lh-display: 0.88` из `tokens.css`), плюс удалить `FloatingGlyphs` и
второй `CodeMarquee`. Освободившееся место — под (а) и (б).

**Как инженерную культуру показывают сильнейшие — с источниками:**

- **Полный исходник портфолио в открытом репозитории** с лицензией и setup-инструкциями. [github.com/brunosimon/my-room-in-3D](https://github.com/brunosimon/my-room-in-3D) — 4.5k звёзд, исходники [bruno-simon.com](https://bruno-simon.com/). Снимает вопрос «а ты правда это написал» — тот самый вопрос, который базовый отчёт §2.8 называет центральной проблемой dev-портфолио.
- **Открытая parameter-панель для посетителя**, а не debug-GUI под флагом. [Tweakpane](https://tweakpane.github.io/docs/) с bindings и monitor-блоками, включая FPS-graph blade. Доказывает, что сцена — система, а не набор захардкоженных чисел. **Прямо применимо к `MotionScreen` и `Modeling3DScreen`.**
- **Кейс как инженерный отчёт про trade-offs.** Не «сделали красиво», а «взяли ogl.js вместо Three.js ради bundle size и multiple render targets, заменили честный рендер стекла на `fract()` + шум». [Codrops / Adrián Gubrica, дек 2025](https://tympanus.net/codrops/2025/12/05/from-illusions-to-optimization-the-creative-webgl-worlds-of-adrian-gubrica/).
- **Поддерживаемая open-source экосистема, а не набор демо.** [pmndrs/react-three-fiber](https://github.com/pmndrs/react-three-fiber) — 31.5k звёзд, drei/rapier/leva, финансирование через Open Collective. Базовый отчёт §2.8 называет это сильнейшим сигналом всей выборки: «выпуск инструмента, которым строят другие разработчики — категориальный статусный скачок». **Проект уже использует drei и rapier — значит, автор внутри этой экосистемы, и это стоит сказать вслух.**
- **Собственный developer tooling как отдельный продукт.** [threejs.org/editor](https://threejs.org/editor/); профиль [mrdoob](https://github.com/mrdoob) — three.js 114k звёзд, `stats.js` 9.1k (буквально монитор производительности).
- **Внешняя валидация по инженерной оси** — [Awwwards Developer Award](https://www.awwwards.com/websites/developer/), а не SOTD вообще.

**Чего избегать:** логосетки техстека (базовый отчёт §2.8, junior-tell №3),
полосок процентов навыков (junior-tell №7), проектов формы туториала.
И отдельно — **узнаваемых готовых компонентов**: cursor-follow blob и magnetic
buttons из ReactBits без изменений. Узнаваемость компонента «из коробки» бьёт
по впечатлению сильнее, чем помогает. `Magnetic` в проекте уже есть — стоит
проверить, что он не читается как библиотечный.

---

### 2.3 3D-моделинг — `Modeling3DScreen.tsx`

**Текущее состояние — самый слабый экран.** 456 строк, пять секций:
hero с ленивым `Model3D` (R3F, `useGLTF` через `import.meta.glob('src/models/*.glb')`,
`ContactShadows`, `Environment` + `Lightformer`, два `useFrame` — турнтейбл и
lerp за курсором), bento-сетка из шести ячеек, шесть шагов пайплайна,
`<dl>` из четырёх позиций, CTA. Палитра «глина» — `#0e0c0a / #181410 / #c9a882`,
Unbounded + Onest.

**Диагноз.** Экран **иллюстрирует** 3D вместо того, чтобы демонстрировать ремесло.
Ни одного слайдера, ни одного тумблера, ни одного до/после, ни одного
переключателя состояния. Из моушна — только `ease.standard`; `ease.entrance`
и `ease.editorial` не используются нигде. Скролл-привязанного движения нет
вообще. Каждая bento-ячейка помечена одним и тем же декоративным ромбом `◆` —
на фоне сконструированного SVG-знака в `BrandingScreen` это плейсхолдер.
И главное: **модель может не отрисоваться вовсе** — она зависит от
пользовательских `.glb`, а фолбэк — голый спиннер. Единственная визуальная
вещь на hero может оказаться пустой.

Плюс bento — сомнительная структура именно здесь (см. Раздел 0): это самая
узнаваемая сетка AI-генерации.

**Три приёма — все про «ремесло, которого не видно на финальном рендере»:**

**(а) Слои пайплайна как персистентное состояние.** Док внизу экрана (прямой
аналог дока палитры в `BrandingScreen`) с переключателем стадий:
`wireframe → retopo → UV → clay → AO → beauty`. Переключение меняет **всё**:
саму модель в hero, превью в сетке, подписи. Почему дорого: финальный рендер
умеет делать генератор картинок; ретопология и развёртка — нет. Показывая
топологию, автор показывает единственную часть работы, которую нельзя
подделать.

**(б) Живые метрики модели.** Не «топология» словом в `<dl>`, а посчитанные
из загруженного GLTF числа: полигонов, вершин, материалов, размер текстур,
плотность текселя, число draw-call'ов. `three.js` отдаёт это из
`renderer.info` и обхода геометрии — считается на клиенте, врать нельзя.
Это прямой аналог живого WCAG-расчёта в `BrandingScreen`.

**(в) Убрать bento, поставить одну модель под свет.** Базовый отчёт §4:
выжившая премиальная форма 3D — «одна твёрдая идея, выполненная чисто:
один объект с настоящим весом, атмосферой и кадрированием, а не
нагромождение эффектов». Референс из того же отчёта — Iventions, где «свет
как основной элемент дизайна, каждый проект подан как объект под точечным
светом». Шесть bento-ячеек с ромбами заменяются на один объект, который
можно вращать, и стадии которого можно переключать.

Плюс обязательное: **фолбэк должен быть не спиннером, а осмысленным
статичным кадром.** Экран не имеет права оказаться пустым.

**Важная поправка к ожиданиям — проверка живых сайтов индустриальных 3D-художников.**
Были проверены сайты действующих художников: Sara Wavreille (моделинг/look-dev,
DNEG), Bryan Bode (creature sculpt, Marvel), Toprak Bekiro, Chris Bleier.
**Ни один не использует real-time GLB-вьюер, wireframe-оверлей, polycount-статистику
или UV-раскладки** — всё это чистые видеорилы и сетки статики.

Вывод из этого не «значит, и не надо», а обратный и более точный:
**интерактивный вьюер на личном сайте — это сигнал позиционирования
tech artist / generalist («умею код + 3D»), а не норма для чистого модельера.
Именно поэтому он и работает как дифференциатор.** Для человека, который на
соседних экранах показывает разработку и моушн, это ровно та подача, которая
связывает дисциплины в одного специалиста, а не в список услуг.

**Инструментарий, подтверждённый документацией:**

- **[`<model-viewer>`](https://modelviewer.dev/)** — AR/WebXR, HitTest API, DOM Overlay. Либо [three-gltf-viewer Дона Маккёрди](https://github.com/donmccurdy/three-gltf-viewer). В проекте уже стоит R3F — можно и своим.
- **Переключение material variants в одном GLB** — расширение `KHR_materials_variants`, `variantName` / `availableVariants`, плюс Materials API для живой правки base color / metalness / roughness: [modelviewer.dev/examples/scenegraph](https://modelviewer.dev/examples/scenegraph/). Доказывает, что UV и шейдинг системные, а не подогнаны под один скриншот.
- **Debug-каналы PBR-разложения** — переключаемые normals / occlusion / metallic-roughness / UV. Мгновенно показывают, понимает ли автор физику материала: [Khronos glTF Sample Viewer](https://github.com/KhronosGroup/glTF-Sample-Viewer). **Это и есть готовая реализация приёма (а) выше.**
- **Живая смена HDRI и tone mapping** — модель, одинаково читающаяся в студийном и экстерьерном свете, доказывает физическую корректность материала. В проекте уже есть `Environment` + `Lightformer` из drei — половина работы сделана.
- **Gaussian Splatting** как флекс современного capture-пайплайна: [superspl.at](https://superspl.at) (WebGPU + streaming), [poly.cam](https://poly.cam). Поправка: у Luma Labs к июлю 2026 брендинг сместился в генеративное видео и AI-агентов, сплаты с главной ушли — как референс Luma устарела.

**Чего избегать:** видеотурнтейбла на чёрном фоне под lo-fi; сетки
превьюшек без единого интерактивного элемента; скриншота «до/после» скульпта
без объяснения ретопологии; HDRI-рендера в вакууме без контекста сцены.

---

### 2.4 Брендинг — `BrandingScreen.tsx` (точечные улучшения)

Экран сделан правильно, переделывать нечего. Четыре точечных усиления:

1. **Показать построение знака в движении, а не тумблером.** Сейчас сетка модулей 12×20, зона отступа и направляющая окружность включаются переключателем. Сильнее — прочертить конструкцию: направляющие появляются по очереди, знак собирается из них, затем направляющие уходят. Один раз при входе секции в кадр, дальше — по кнопке «ещё раз». Длительность по базовому отчёту §3a для editorial-раскрытия: `1000–1400ms`, `ease.editorial [0.65,0,0.35,1]`.
2. **Добавить в док палитры значение контраста прямо на кнопку.** WCAG уже считается на экране; вынести результат на сам переключатель — и станет видно, что палитры отличаются не вкусом, а проходимостью. Дисквалифицирующая палитра, оставленная в наборе с честной пометкой, — сильный ход.
3. **Живой специмен вместо статичного.** Секция 03 показывает Instrument Serif и Onest образцами. Добавить слайдер кегля с пересчётом трекинга — механика уже написана в `WebDesignScreen` (`tracking = -0.005 - t*0.04`), переиспользуется.
4. 🔴 **Заменить Instrument Serif — у него нет кириллицы.** Проверено по метаданным Google Fonts: только `latin` / `latin-ext`. Русские заголовки экрана сейчас рендерятся подстановочным шрифтом. Плюс базовый отчёт §5 каталогизирует его как перегруженный AI-слопом. Замена: Cormorant Infant 600 (бесплатно) или GT Sectra / CoFo (платно). См. Раздел 5.

**Что усиливает брендинговый кейс в 2026 — с источниками:**

- **Motion identity как система с именованной грамматикой**, а не зациклённый ролик. DixonBaxi / TNT Sports — «Viewfinder», *«a dynamic graphic device born from the two Ts in the logo»*: [dixonbaxi.com/case-study/tnt-sports](https://dixonbaxi.com/case-study/tnt-sports). Instagram × Studio Dumbar/DEPT — «flexible motion techniques» плюс guidelines и toolkits: [deptagency.com](https://deptagency.com/insight/instagram-reveals-new-dynamic-motion-identity-system/). **Прямо применимо: у экрана уже есть сконструированный знак на модульной сетке — из него можно вывести именованный graphic device.**
- **Параметрическая система, поданная как движок.** Pentagram / Mastercard — на странице кейса буквально *«the parametric design generates infinite arrangements»*: [pentagram.com/work/mastercard](https://pentagram.com/work/mastercard). Для клиента это прямая экономия: бренд масштабируется без повторных обращений.
- **Один точный, почти абсурдный process-артефакт** вместо стены отвергнутых логотипов. DixonBaxi / TikTok — *«an unexpected constraint — biting off a corner — to clean up how the brand reads»*, решающее *«no more double vision»*. Такая деталь читается подлинной именно потому, что слишком специфична, чтобы быть придуманной постфактум.
- **Явный «language bridge» между айдентикой и продуктовым UI.** Collins / Bose — *«a signature animation style that transforms static brand elements into instantly recognizable kinetic expressions»*: [wearecollins.com/case-studies/bose](https://wearecollins.com/case-studies/bose). Снимает главный страх заказчика — выживет ли система в реальном софте.

**Чего избегать:** изометричных device-мокапов (телефон + ноутбук + тоут-бэг
под углом на цветном фоне); flatlay «brand in the wild» с кофейной чашкой;
нумерованного процесса 01→02→03 в одинаковых кружках (базовый отчёт §4
отдельно помечает `01 / 02 / 03` как признак AI-генерации — **на этом экране
секции пронумерованы именно так, стоит пересмотреть**); скриншотов PDF
brand guidelines вместо живой токен-системы; грейскейл-сеток logo exploration
без строчки обоснования.

---

### 2.5 Веб-дизайн — `WebDesignScreen.tsx` (точечные улучшения)

Эталон. Три точечных усиления:

1. **Клавиша `G` объявлена, но одинока.** Раз механика горячих клавиш есть, стоит достроить до маленькой системы: `G` — сетка, `B` — бейзлайн отдельно от колонок, `O` — оптические поля. И один раз показать подсказку. Набор шорткатов — это сигнал инструмента, а не страницы.
2. **Секция 04 «состояния компонента» — шесть состояний кнопки.** Усиление: показать не только состояния, но и **их тайминги** — сколько миллисекунд занимает каждый переход, с реальными значениями из `motion.ts` (hover `--d-fast 240ms`, нажатие `--d-instant 120ms`). Это связывает экран веб-дизайна с экраном моушна и показывает, что система одна.
3. **Оживить `.grain`.** Экран светлый (`#f4f3ef`), зерна нет вообще. На бумажном фоне зерно `0.03–0.05` — это ровно та разница между «белый div» и «печатная поверхность», ради которой оно и написано.

**Два подтверждённых незанятых окна** — это важнее трёх пунктов выше.
Исследование целенаправленно проверило, есть ли эти вещи у лидеров, и
обнаружило, что **их нет почти нигде**:

- **Живой design-token / component explorer внутри кейса.** Проверены [vercel.com/geist/colors](https://vercel.com/geist/colors) — только «Right click to copy raw values», ни живых свотчей, ни переключателя тем; и [instrument.com/work/nike-digital-design-system](https://instrument.com/work/nike-digital-design-system) — «flexible component library» упомянута прозой, без единого показанного токена. **Если этого нет даже у Vercel и в топовом кейсе про Nike — живой explorer остаётся редкостью и потому высоким сигналом.** На этом экране половина уже есть (живая шкала кегля, состояния компонента) — не хватает шага до полноценного проводника по системе.
- **Публичные process-артефакты.** Проверено 7 крупных студийных кейсов ([obys.agency](https://obys.agency/), [14islands / Neko Health](https://14islands.com/work/neko-health), Instrument / Nike, [antinomy.studio / Google Gemini](https://antinomy.studio/project/google-gemini), подстраницы basement.studio, [portorocha.com](https://portorocha.com)) — **ни в одном** нет вайрфреймов, отвергнутых направлений или моушн-спеков на публично отданной странице. Один реальный артефакт закрывает разрыв, который не закрывает почти никто.

**Приёмы подачи, подтверждённые живыми примерами:**

- **Живые встроенные демо компонентов — настоящий инстанс, не скриншот.** [joshwcomeau.com/css/interactive-guide-to-flexbox](https://joshwcomeau.com/css/interactive-guide-to-flexbox/) — draggable «Container width: 600 — Drag me», живой инкремент `flex-grow`, встроенная песочница. [emilkowal.ski/ui/building-a-toast-component](https://emilkowal.ski/ui/building-a-toast-component) — живые инстансы Sonner со стеком hover-to-expand, swipe-to-dismiss и pointer capture прямо в тексте. **Это единственное, что невозможно подделать текстом.**
- **Отдельный датированный craft-журнал микровзаимодействий** как постоянный пункт навигации, а не подраздел кейса. [rauno.me/craft](https://rauno.me/craft) — записи 2021→дек 2025: Combobox, Precision Slider, Radial Menu, Gooey Shader, Blur Reveal. [paco.me/craft](https://paco.me/craft) — «Timeline from Linear with pure CSS», «Custom player controls and keyboard shortcuts». Показывает **непрерывный выпуск**, а не разовый редизайн портфолио под поиск работы. Для позиционирования «недоступности» это сильно: человек, который выкладывает эксперименты годами, очевидно не ищет работу.
- **Императивная тестируемая подпись вместо настроенческой копии.** Формат «Notice how…», «Try dragging…» — второе лицо, проверяемое утверждение, привязанное к живому элементу рядом. Превращает страницу в инженерную документацию.
- **Конкретные числа, вшитые в прозу, а не в дашборд.** Sonner — «40+ million weekly npm downloads»; Porto Rocha / Netflix Tudum — «78 million livestreams»; 14islands / Neko Health — «sold out every clinic spot within hours of launch». Это прямая замена секции `Stats` со счётчиками.

**Главное клише, доказанное прямыми находками — «prose-poem кейс»:**
full-bleed hero + два-три предложения настроенческой прозы + credits
(client / year / scope) + «visit live site», без единого интерактивного
доказательства. Один и тот же шаблон найден на четырёх подстраницах
[basement.studio/showcase](https://basement.studio/showcase), у Antinomy,
14islands и Jordan Gilroy. **Это уже не premium, а baseline 2025–2026,
считываемый как «как у всех».**

---

## РАЗДЕЛ 3. Аудит текущего состояния — что уже есть и куда это двигать

### 3.1 Что в коде уже сделано правильно

Система токенов (`src/design/tokens.css`, `src/design/motion.ts`) — сильная,
её не надо переделывать, надо **использовать полнее**:

- Нейтральная шкала уведена в тёплое (`#060505 → #f7f4ec`), не в серое. Это ровно то, что `trends-2026.md` §2.3 называет разницей между «дорогим чёрным» и дефолтным `#000`.
- Один акцент, три светлоты (`--a-dim #c23a17` / `--a #ef4a23` / `--a-bright #ff6a3d`) + альфа-производные. Дисциплина 60/30/10 соблюдена.
- Обратная зависимость трекинга от кегля (`--tr-display -0.045em` → `--tr-body -0.005em`).
- **Комментарий про кириллицу в `--lh-display: 0.88`** — про то, что «й» и «ё» поднимаются выше прописной высоты и срезаются при плотном интерлиньяже. Это редкая по грамотности вещь, её надо распространить на все экраны (сейчас явно учтено только в `WebDesignScreen` и `BrandingScreen`).
- `.optical-left { margin-left: -0.055em }` — оптическая компенсация левого края.
- Дисциплина `will-change` через класс `.animating`, а не постоянно.
- `[data-idle='true']` + `useOffscreenPause` — пауза анимаций вне кадра.

### 3.2 Мёртвый код, который надо оживить

**`.grain` в `tokens.css:237` не используется НИ НА ОДНОМ экране.** Фильтр
`feTurbulence baseFrequency='0.82' numOctaves='3' stitchTiles='stitch'`,
`opacity: 0.035`, `mix-blend-mode: overlay` — рецепт корректный (совпадает с
каноническим из §3.1 базового отчёта). Это готовая общая визуальная подпись,
которая сейчас просто не подключена. См. Раздел 6 про то, где её включать.

### 3.3 Что делает `WebDesignScreen` и `BrandingScreen` эталонными

Четыре свойства, которых нет у трёх остальных экранов:

1. **Персистентное состояние, которым управляет посетитель, и оно расходится по всему экрану.** В `BrandingScreen` — фиксированный док палитры внизу: переключаешь одну из пяти палитр (terra/indigo/pine/plum/coal), и одновременно перекрашиваются знак, свотчи, визитка, аватар, вывеска, обложка. В `WebDesignScreen` — клавиша `G`, включающая 12-колоночный оверлей и 8px-бейзлайн поверх всего экрана.
2. **Числа, посчитанные на странице, а не заявленные.** `BrandingScreen` считает WCAG-контраст живьём (`luminance()` / `contrast()` / `grade()`). `WebDesignScreen` считает трекинг и интерлиньяж как функцию кегля: `tracking = -0.005 - t*0.04`, `leading = 1.62 - t*0.66`, при живом слайдере базового кегля 13–22px и пяти модульных отношениях (1.125 / 1.2 / 1.25 / 1.333 / 1.5).
3. **Собственная типографическая шкала экрана, а не только `var(--t-*)`.** `WebDesignScreen`: Archivo 400–800 + IBM Plex Mono, `h1 clamp(3rem, 12vw, 10.5rem) / -0.05em / 0.95`. `BrandingScreen`: Instrument Serif + Onest, `h1 clamp(3.2rem, 12vw, 9.5rem) / -0.045em / 0.92`. Экран получает собственный голос.
4. **Объявленная посетителю сигнатурная аффорданса.** Клавиша `G`; док палитры. Пользователю прямо показывают, что здесь есть, чем поиграть.

**Это и есть формула, которую надо перенести на три остальных экрана.**
Не «добавить эффектов» — а: одно персистентное состояние + одно живое
вычисление + собственная шкала + одна объявленная аффорданса.

---

## РАЗДЕЛ 3-БИС. Аудит главной страницы

Текущий порядок секций (`src/App.tsx`), с авторским комментарием-воронкой
«Кто → чем занимаюсь → почему мне можно верить → что получите → доказательство
→ как это устроено → что говорят → возражения → просьба»:

`Hero → Marquee → About → Stats → Services → Projects → Process → Testimonials → FAQ → Contact`

### Что не так со структурой — по мерке базового отчёта

`trends-2026.md` §2.3 установил каноническую премиальную последовательность:
**hero → WORK немедленно → кто мы → услуги коротко → пруфы консолидированно →
футер-как-CTA**, и зафиксировал: «Work перед services — практически
универсально… Services-first — паттерн низкого чека».

**Текущая главная нарушает это дважды:**

1. **Projects стоит шестым, после About, Stats и Services.** Между hero и
   первой работой — четыре секции разговоров о себе. Это ровно инверсия
   премиального паттерна.
2. **Stats (счётчики) стоит третьим** — то есть плотность пруфов вынесена
   наверх. Базовый отчёт §2.3: «Плотность пруфов обратно коррелирует с ценой.
   Уверенность структурно тиха».

**Рекомендуемый порядок:**

```
Hero → Marquee (короткий, как выдох) → Projects/«портфолио по запросу»
  → About → Services → Process → Stats (сжатые, в строку, не счётчики)
  → FAQ → Contact
```

Projects сейчас показывает пустое состояние `Портфолио\nпо запросу` —
и это **не проблема, а актив**, если поставить его вторым экраном. Строка
«Большая часть работ закрыта соглашением о неразглашении и не публикуется» на
позиции №2 работает как приём недоступности (Раздел 1.1). На позиции №6 она
работает как извинение.

### Секции, которые читаются как шаблон

По результатам аудита кода — четыре слабых места, в порядке серьёзности:

| Секция | Проблема | Куда двигать |
|---|---|---|
| **StatsSection** | Четыре count-up счётчика в сетке с волосяными разделителями. Самый распространённый паттерн портфолио в природе. Плюс сами цифры помечены в `content.ts` как `⚠️ ЗАПОЛНИТЬ РЕАЛЬНЫМИ ЗНАЧЕНИЯМИ`. | Убрать счётчики совсем. Базовый отчёт §2.4: «Цифры, а не бейджи… набраны собственным шрифтом сайта». Формат Locomotive — голое число в скобках внутри строки прозы, без анимации: `Awards & Recognitions (295)`. Одна строка вместо секции. |
| **FaqSection** | Учебниковый аккордеон: нумерованные строки, плюс, который поворачивается в крестик. Реализация через `grid-template-rows: 0fr → 1fr` за 450ms — технически отличная, но **форма** абсолютно стандартная. | Форма важнее реализации. Вариант: не аккордеон, а разворот в две колонки — вопрос слева крупно, ответ справа всегда открыт. Возражения, поданные открыто, читаются увереннее, чем спрятанные под плюсик. |
| **TestimonialsSection** | Цитаты-карточки в 3 колонки с крупной фоновой кавычкой — каноническая раскладка отзывов. Сейчас секция возвращает `null` (нет реальных данных). | Базовый отчёт §2.4 прямо: «Отзывы почти полностью отсутствуют наверху рынка… логотип клиента И ЕСТЬ отзыв». **Не заполнять. Удалить секцию.** |
| **ProcessSection** | Вертикальный таймлайн с точками и заливающейся линией. Скролл-привязанный градиентный рельс (`useScroll offset ['start 75%','end 65%']` → `useSpring {140,30,0.9}`) — достойный апгрейд, но база всё равно общая. | Оставить, но убрать длительности этапов (`3–5 дней`… `1 неделя`). Названные сроки — сигнал подрядчика, а не автора. |

### Ритмическая проблема

Три подряд идущие секции — Process, Testimonials, FAQ — имеют одинаковый
такт «лейбл + заголовок + список». В исходниках это осознано, и раскладка
варьируется (одна колонка / сетка / асимметричная пара), но **бит один и тот же**.
Лечение — в Разделе 1.3: между ними нужна не другая раскладка, а другая
температура и другая плотность.

### Самая слабая точка всего сайта

**Переход между главной и пятью экранами услуг не анимирован вообще.**
`App.tsx:54–64` — обычный тернарник, без `AnimatePresence`, без exit-анимации,
только `requestAnimationFrame(() => jumpToTarget(0))` и CSS-фейд `screen-in`
`opacity 0→1, 0.5s ease`. В остальном моушн-система выстроена тщательно, и
именно в самый драматургически заряженный момент — когда посетитель выбирает
дисциплину — сайт делает джамп-кат. См. Раздел 4.

### Второе по слабости

**Hero-канвас — единственная сигнатурная графика на всей главной.**
`ReactiveGrid` (2D canvas, `SPACING 46px`, `RADIUS 190px`, DPR clamp до 2,
rAF под `IntersectionObserver`) сделан аккуратно и дёшево. Но после первого
экрана визуальный интерес падает обрывом: всё остальное — типографика плюс
волосяные линии. Это дисциплина, но не погружение. См. Раздел 1.2 и 1.3.

### Мелочи, которые стоит закрыть

- `SERVICES_COPY.subtitle` в `content.ts` не используется: `ServicesSection.tsx:316–319` хардкодит свой вариант. Токен дрейфует.
- 3D-остров в `ContactSection` — самый тяжёлый ассет страницы, стоит между формой и футером и тематически оторван от момента «нанять». Честная подводка `Пока вы здесь` спасает не полностью.
- Портрет в `AboutSection` — SVG-плейсхолдер с подписью «Сюда портретное фото». На живом сайте читается как незаконченность.

---

## РАЗДЕЛ 4. Переходы между секциями и экранами

Это **самая недоработанная часть проекта** и одновременно самая дешёвая в
исправлении: приёмы ниже не требуют новых зависимостей.

### 4.1 Проблема

`App.tsx:54–64` переключает пять экранов услуг обычным тернарником. Нет
`AnimatePresence`, нет exit-анимации, есть `requestAnimationFrame(() => jumpToTarget(0))`
и CSS-фейд `screen-in` (`opacity 0→1, 0.5s ease`). Роутера нет — это
одностраничное приложение с состоянием `screen`.

Драматургически это худший возможный момент для джамп-ката: посетитель только
что выбрал дисциплину, то есть проявил максимальное намерение за весь визит.

### 4.2 Что применимо на этом стеке

**View Transitions API — поддержка перестала быть проблемой, но здесь всё
равно не подходит. Причина архитектурная.**

Актуальный статус (проверено по caniuse и MDN, июль 2026): **Chrome/Edge 111+,
Safari 18.0+, Firefox 144+** (в 143 требовался флаг), Opera 97+; мобильные —
Chrome for Android 150+, iOS Safari 18.0+, Samsung Internet 23+.
**Глобальный охват 88.46%.** Это принципиальная перемена относительно 2024
года, когда API был «только Chrome» — базовый отчёт этого не фиксирует.

Почему всё равно не сюда: кросс-документные переходы
(`@view-transition { navigation: auto }`, правило должно стоять **в обоих**
документах) требуют настоящей навигации между документами, а здесь SPA без
роутера. Same-document через `document.startViewTransition()` технически
возможен, но конфликтует с `AnimatePresence`: обе системы хотят держать старое
и новое дерево одновременно. MDN отдельно предупреждает про доступность —
одновременное присутствие старого и нового содержимого ломает позицию чтения,
путает фокус и мешает объявлению live-регионов. Плюс **вложенные вызовы
`startViewTransition()` дают непредсказуемое поведение**.

**Вывод: на этом проекте переходы делаются на framer-motion, который уже
стоит.** Если появится роутер — вернуться к вопросу; тогда пригодятся
`view-transition-class` (снимает комбинаторный взрыв правил — в отличие от
`view-transition-name`, не обязан быть уникальным) и события
`pageswap` / `pagereveal`. Полезно знать про потолок: **таймаут перехода 4
секунды**, только главный фрейм, типы не переносятся автоматически между
страницами.

**Шторка (curtain) — основной приём, максимум эффекта на минимум кода.**
Механика уже существует в проекте: `LoadingScreen` уходит через `y: '-100%'`
за `duration.slower` (0.95s) с `ease.editorial [0.65,0,0.35,1]`. Тот же
компонент переиспользуется как переход между главной и экраном:

```
1. клик по услуге
2. шторка входит снизу:  y: '100%' → '0%'   340ms  ease.exit [0.7,0,0.84,0]
3. под шторкой: смена screen + jumpToTarget(0)   (0ms, невидимо)
4. шторка уходит вверх:  y: '0%' → '-100%'  620ms  ease.editorial
```

Асимметрия обязательна: вход быстрый и резкий (`ease.exit` — ускоряющаяся
кривая), уход медленный и мягкий (`ease.editorial`). Базовый отчёт §3a даёт
для editorial-шторок диапазон **1000–1400ms** на полный цикл — 340 + 620 = 960ms
плюс кадр на смену состояния попадает точно в него.

Усиление за три строки: на шторке — название выбранной дисциплины,
набранное `--t-h1`, входящее и уходящее вместе со шторкой. Тогда переход не
скрывает смену, а **объявляет** её.

**Морфинг общего элемента (shared element).** Пять строк услуг в
`ServicesSection` — это готовые исходные точки. При клике строка
(`3D-моделинг`, `Разработка`, …) не исчезает, а переезжает в позицию
заголовка целевого экрана. На framer-motion это `layoutId`:

```tsx
// в ServicesSection
<motion.h3 layoutId={`svc-${id}`}>{title}</motion.h3>
// в целевом экране
<motion.h1 layoutId={`svc-${id}`}>{title}</motion.h1>
```

Требует общего `AnimatePresence` и `LayoutGroup` на уровне `App.tsx`.
Почему читается дорого: связь между «я нажал вот на это» и «вот куда я попал»
становится физической, а не логической. Это тот самый приём, из-за которого
переходы у топ-студий описывают как «движения камеры, а не анимацию
элементов» (базовый отчёт §1.2, разбор Uncommon Studio).

Важно: `layout`-анимации framer-motion считаются на главном потоке и могут
дёргаться на длинных списках. Здесь список из пяти строк — безопасно.

**Sticky-сцены.** Уже используются и работают: стопка карточек проектов
(`targetScale = 1 - (total-1-index)*0.03`, `offset: ['start end','start start']`),
шесть карточек этапов в `DevelopmentScreen`, кольцо технологий на 320vh,
горизонтальная галерея на 280vh в `MotionScreen`. Все — на `useScroll` +
`useTransform` поверх `position: sticky`, без GSAP ScrollTrigger. Это
правильный выбор для React: sticky нативен и не ломается при ресайзе.

**Про нативные scroll-driven animations (`animation-timeline: scroll()/view()`).**
Поддержка уже приличная: **Chrome/Edge 115+, Firefox 155+, Safari 26.0+**
(десктоп и iOS одновременно), Samsung Internet 23+ — **глобально 83.66%**.
MDN всё ещё маркирует свойство как «not Baseline», но технология пригодна к
продакшену с прогрессивным улучшением. Главное преимущество перед JS —
анимации уходят **с главного потока**.

**Здесь всё равно не применять**, по одной конкретной причине: Lenis работает
через трансформацию контейнера, а нативный `scroll()` timeline привязан к
настоящей позиции скролла. Их сочетание даёт рассинхрон. Проект уже сделал
выбор в пользу Lenis (`lerp: 0.085`); значит, скролл-привязанное движение
остаётся на `useScroll` из framer-motion, который читает то же значение, что и
Lenis. **Это и есть тот «названный компромисс», который стоит вынести на
`DevelopmentScreen`** (Раздел 2.2б) — настоящее решение с настоящей ценой.

Две ловушки на будущее, если Lenis когда-нибудь уберут:
- `animation-timeline` — **reset-only** значение в шорткате `animation`. Объявлять его строго **после** шортката, иначе молча перестаёт работать.
- Если именованный таймлайн не найден, анимация просто не привязывается — без ошибки и без предупреждения.
- Полифилл: `flackr/scroll-timeline`; эталонная витрина приёмов — `scroll-driven-animations.style`.

### 4.3 Переходы внутри страницы

- **Якорная навигация** уже проходит через `scrollToTarget` (`duration: 1.4`, `easeOutCubic`) — это хорошо и соответствует норме §3a для перемещений на полный вьюпорт.
- **Границы секций.** Сейчас переходы между секциями оформлены скруглением верхних углов (`--r-2xl: clamp(28px, 4vw, 56px)`, у Services 40/50/60px). Базовый отчёт §5 помечает крупные скругления (24px+, особенно 44px) как признак AI-генерации. **Здесь это скругление секции, а не карточки, так что прямого попадания нет** — но 60px близко к границе. Рекомендация: скругление оставить только на единственной инвертированной секции, где оно работает как «лист бумаги, наложенный сверху», и убрать со всех остальных.
- **Курсор.** `CustomCursor` смонтирован глобально, `CursorRing` — локально в `MotionScreen` с `cursor: none`. Базовый отчёт §2.8 (junior-tell №9): «кастомный курсор, ухудшающий юзабилити» — эффект без нагрузки за ним. Проверить: курсор должен что-то **делать** (менять форму над интерактивным, показывать подсказку), а не просто быть кружком-с-задержкой.

---

## РАЗДЕЛ 5. Кириллическая типографика

**Это главный пробел базового отчёта.** В нём перечислено 60+ гарнитур, и
почти ни одна из них не решает задачу русскоязычного сайта. Apercu, GT America,
Founders Grotesk, Canela, Ogg, Söhne, Editorial New, Migra, Reckless,
Tiempos, Domaine, GT Sectra — **кириллицы либо нет вовсе, либо она есть только
в дорогих расширенных пакетах**, которые надо проверять по конкретной лицензии,
а не по названию семейства.

> ⚠️ Покрытие кириллицы у каждой конкретной гарнитуры **обязательно проверять на
> странице словолитни перед покупкой**. Название семейства ничего не гарантирует:
> у многих зарубежных шрифтов кириллица есть только в отдельном «Cyrillic»
> или «Pro/Extended» пакете, и цена отличается кратно.

### 5.1 Что уже стоит на сайте

**MTS Wide**, самохост, начертания 300/400/500/700, `woff2`, Bold и Regular
в `<link rel=preload>` в `index.html`, `font-synthesis: none`. Одна гарнитура
на всю главную.

**Это сильное решение, и вот почему.** Базовый отчёт §1.3 приводит данные
Typewolf по 40 топовым студийным сайтам: **12 из 40 используют ровно один
шрифт** (Wade and Leta — Söhne, Morrow — Untitled Sans, Cast Iron Design —
Unica77). Одна гарнитура на весь сайт — это статусный жест, а не экономия.
Плюс `font-synthesis: none` — редкая по грамотности деталь: браузер не имеет
права подделывать жирность и курсив, а поддельный «болд» кириллицы выглядит
особенно плохо.

Плюс MTS Wide решает главную проблему сразу: **кириллица у него родная**,
не адаптированная, с нормальными пропорциями.

**Риск позиционирования.** MTS Wide — корпоративный шрифт конкретного бренда.
Аудитория, знакомая с российским рынком, его узнаёт, и на портфолио
независимого специалиста это может прочитаться как «взял чужой фирменный
шрифт». Проверить лицензию на коммерческое использование вне бренда —
обязательно, до всего остального.

### 5.2 Куда двигать типографику главной

Формула из базового отчёта §1.3, адаптированная под кириллицу:

```
display (акцидентный)  +  neo-grotesque (текст)  +  mono (метаданные)
```

Mono здесь — это код-сигнал: он говорит «я разработчик» без единого слова.
На главной сейчас mono-начертания нет вообще (`JetBrains Mono` подключён,
но заскоплен на экран моушна), при том что `.t-mono` и `--tr-mono: 0.32em`
в токенах определены. Это ещё один кусок мёртвого CSS.

### 5.2-БИС Проверенное покрытие кириллицы — по словолитням

Проверка велась по метаданным Google Fonts (`fonts.google.com/metadata/fonts/<Family>`)
и по страницам конкретных гарнитур, **не по каталогам** — каталог обычно
молчит о письменностях.

**Главный практический вывод: половина «дорогих» шрифтов из базового отчёта
для русского сайта недоступна.**

| Словолитня | Кириллица | Что это значит |
|---|---|---|
| **Contrast Foundry** (contrastfoundry.com) | **Есть во всём каталоге**, почти всё вариативное, **от $70** | Самый однозначный результат проверки. CoFo Sans Global (95 начертаний, 9 письменностей), CoFo Gothic (12), CoFo Robert Sans (7), CoFo Weather (60), CoFo Drifter (от $42). Сигнал — современная московская школа. |
| **type.today** | **215 семейств**, кириллический дистрибьютор №1 | Розничные начертания $50–60; вариативные версии **$1300–1500** — это разные лицензионные тиры, не опечатка. Свежее: Crimp, Halvar Mono, Fluxetype. |
| **Brownfox** | 21 семейство, все **от €50** | Formular, Geometria, Ferro Grotesk, Gerbera, Nolde. Моно-версии есть у Formular, Aeroport, Statika, Coil, Antonym. |
| **TypeType** | 75 шрифтов, 46 вариативных, кириллица — норма каталога, **от $47.99** | TT Norms Pro (104 стиля), TT Commons Pro, TT Hoves Pro, TT Neoris Pro. Безопасно и дёшево — но TT Norms Pro настолько распространён, что премиальности уже не сигнализирует. |
| **Pangram Pangram** | **Не сквозная — проверять поштучно** | **PP Neue Montreal — кириллица подтверждена** (506 языков, русский/украинский/сербский/болгарский/македонский; 36 начертаний, вариативные, от $40, v3.00 апрель 2026). Коллекции `/collections/cyrillic` не существует. |
| **Grilli Type** | **Ровно пять семейств** | **GT America**, **GT Pressura**, **GT Sectra**, **GT Eesti**, **GT Walsheim** — есть. GT Alpina, GT Flexa, GT Maru, GT Ultra — **нет**. |
| **Klim** (Söhne, Tiempos, Domaine, Founders Grotesk…) | **Не подтверждена** — страницы не раскрывают языковую поддержку | ⚠️ Разрушает сценарий «возьмём Söhne, как у всех дорогих студий». Для русского проекта **не закладываться** без прямого запроса в словолитню. |
| **Displaay** (Reckless, Teodor, Tobias, Greed, Haffer, Saans) | **Проверенный Saans — кириллицы НЕТ** (116+ языков, все латинские) | Остальной каталог по умолчанию считать без кириллицы. |
| **Letters from Sweden** | Не документирована ни у одного из 45+ шрифтов | Требует прямого запроса. |
| **ABC Dinamo** (включая ABC Diatype) | **Не подтверждена** | Гипотеза о наличии у части каталога остаётся гипотезой. |
| **Fontfabric** (Болгария) | Есть у Calleo, Code Next; $12–45 | Доступный тир. Bebas Neue кириллицу имеет, но использовать нельзя — см. 5.4. |
| **Rentafont** | 6500+ шрифтов, сильный кириллический фокус | Уникальная модель: **веб-подписка с оплатой за показы страниц**. Для портфолио с малым трафиком — реально дёшево. |
| **CSTM Fonts** | Не проверено — `cstm.cc` не резолвится, `cstmfonts.com` отдаёт 401 | — |

### 5.3 Бесплатные — проверено по метаданным Google Fonts

**Кириллица ПОДТВЕРЖДЕНА:**

| Шрифт | Покрытие | Оси | Комментарий |
|---|---|---|---|
| **Onest** | cyrillic, cyrillic-ext, latin, latin-ext | wght 100–900 | Уже используется в `BrandingScreen` и `Modeling3DScreen`. Лучший свободный нейтральный гротеск с кириллицей. |
| **Golos Text** | cyrillic, cyrillic-ext, latin, latin-ext | wght 400–900 | Безупречная кириллица, но институциональная аура (делался для госсервисов). |
| **Unbounded** | cyrillic, cyrillic-ext, latin, latin-ext, vietnamese | wght 200–900 | 1300+ глифов. Уже используется в `Modeling3DScreen`. |
| **Manrope** | latin, latin-ext, greek, cyrillic, cyrillic-ext | wght 200–800 | Кириллица есть, вопреки частому заблуждению. Но проходит пик популярности. |
| **Geist** | cyrillic, cyrillic-ext, latin, latin-ext, vietnamese | wght 100–900 + курсивы | **У Geist есть кириллица** (диапазоны 1024–1119, 1168–1169). Самая полезная находка: базовый отчёт хвалит Geist как dev-шрифт, и он оказывается применим на русском. |
| **Cormorant Infant** | latin, latin-ext, cyrillic, cyrillic-ext | wght 300–700 | Единственный свободный сериф с кириллицей, способный нести дисплейную нагрузку. **На тёмном фоне тонкие штрихи съедаются — брать 500–600, не 300.** |
| **Wix Madefor Display** | latin, latin-ext, cyrillic, cyrillic-ext | wght 400–800 | Есть. |

**Кириллица ОТСУТСТВУЕТ:**

- ⚠️ **Instrument Serif** — только `latin` и `latin-ext`, все 200+ языков с суффиксом `_Latn`, вариативных осей нет, два начертания. **Для русского сайта не работает вообще.** Это критично: он стоит в `BrandingScreen`. Двойной повод заменить — базовый отчёт §5 ещё и каталогизирует его как перегруженный AI-слопом.

**Не подтверждено (не считать фактом):** Bounded и Involve в Google Fonts
отсутствуют (404). PT Root UI — кириллица почти наверняка есть (Paratype), но
не проверена. Nyghtserif, e-Ukraine, Steinbeck — не проверены. **Yandex Sans** —
корпоративная гарнитура, свободной лицензии на общее использование, судя по
всему, нет; без юридической проверки не закладываться. **JetBrains Mono и
Geist Mono** — кириллица не проверена (проверен только Geist Sans).

### 5.4 Что заезжено в русском премиум-вебе

*(профессиональное суждение, не измеренные данные)*

**Montserrat** и **Roboto** — давно за пределами обсуждения. **Inter** — стал
невидимым дефолтом: не ошибка, но и не заявление. **Bebas Neue** — 2015 год в
чистом виде. **TT Norms Pro** — отличный шрифт, ставший корпоративным дефолтом
до полной нейтральности. **Circe** и **Museo Sans Cyrl** — маркер середины
2010-х. **Graphik** и **Suisse Int'l** — были премиум-сигналом 2018–2021,
сейчас читаются как «студийный шаблон». Из бесплатных пик популярности прямо
сейчас проходят **Manrope** и **Unbounded**.

### 5.5 Три сценария пар для тёмного русского портфолио

Все гарнитуры — с **подтверждённой** кириллицей.

**A — платный, максимальный сигнал (~$250–400).**
Дисплей: **CoFo Gothic** или **CoFo Robert Sans** (Contrast Foundry, от $70, variable).
Текст: **PP Neue Montreal Text** (от $40, variable).
Моно: **GT Pressura Mono** (Grilli Type) или **Halvar Mono** (type.today, от $60).
Сигнал: московская школа + международный гротеск. Не провинциально и не шаблонно-глобально.

**B — платный, редакторский (~$200–300).**
Дисплей: **GT Sectra** — «режущая» антиква, кириллица подтверждена.
Текст: **CoFo Sans Global** или **Onest**.
Моно: **Halvar Mono**.
Сигнал: издательский, кураторский. На тёмном работает, потому что высокая контрастность штриха Sectra читается как намеренная.

**C — полностью бесплатный, но не дешёвый на вид ($0).**
Дисплей: **Unbounded** 300–400 в очень крупном кегле, либо **Cormorant Infant** 600.
Текст: **Onest** 400/500.
Моно: **Formular Mono** (Brownfox, €50) — единственный вариант с гарантией; JetBrains Mono и Geist Mono по кириллице не проверены.

**Фолбэк-стеки.** На тёмном фоне подмена шрифта особенно заметна, поэтому
деградировать надо в кириллические системные, а не в Times:

```css
--font-display: "CoFo Gothic", "Unbounded", "Helvetica Neue", Arial, sans-serif;
--font-body:    "PP Neue Montreal", "Onest", -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
--font-mono:    "Halvar Mono", "GT Pressura Mono", ui-monospace, "SF Mono", "Cascadia Mono", Consolas, monospace;
```

**Два требования тёмной темы, которые важнее выбора гарнитуры:**
1. `font-synthesis: none` — уже стоит в проекте. Браузер не должен «дорисовывать» отсутствующие кириллические жирные и курсивные начертания: на тёмном синтетика выглядит грязно.
2. На тёмном фоне текст оптически тяжелеет. Брать вес **на ступень ниже** привычного (400 вместо 500) и слегка увеличивать трекинг мелкого текста, порядка `letter-spacing: 0.01em`.

### 5.6 Что проверить в текущих экранах

Экраны услуг подключают свои гарнитуры, и часть из них по кириллице
проблемна:

| Экран | Гарнитуры | Статус |
|---|---|---|
| `WebDesignScreen` | Archivo 400–800 + IBM Plex Mono | Кириллица есть у обеих. Безопасно. |
| `BrandingScreen` | **Instrument Serif** + Onest | 🔴 **Критично. У Instrument Serif кириллицы НЕТ вообще** (проверено по метаданным Google Fonts: только `latin` / `latin-ext`). Русские заголовки экрана сейчас рендерятся подстановочным шрифтом. Плюс базовый отчёт §5 каталогизирует его как перегруженный AI-слопом. **Заменить: Cormorant Infant 600 (бесплатно) или GT Sectra / CoFo (платно).** |
| `MotionScreen` | **Bricolage Grotesque** + Geist Mono | ⚠️ Кириллицу Bricolage проверить. Geist Sans кириллицу имеет, Geist **Mono** — не проверен. |
| `DevelopmentScreen` | **Handjet** + JetBrains Mono | ⚠️ Handjet — пиксельный/вариативный акцидентный, кириллица у него есть, но как основной шрифт экрана он читается как игровой, а не инженерный. Не соответствует задаче «показать инженерную культуру» (Раздел 2.2). Заменить. |
| `Modeling3DScreen` | Unbounded + Onest | Кириллица есть у обеих. Безопасно. |

**Отдельно — кириллическая метрика.** В `tokens.css:87–92` уже есть верный
комментарий: «й» и «ё» поднимаются выше прописной высоты, и при плотном
интерлиньяже верхняя строка срезает диакритику; `0.88` — минимум. Явно этот
фикс применён только в `WebDesignScreen` (`lineHeight 0.95` с комментарием)
и `BrandingScreen` (`0.92`). **Три остальных экрана его не документируют**, а
`MotionScreen` использует `0.88` в hero — то есть ровно на границе. Проверить
на реальных заголовках с «й» и «ё».

Второй кириллический нюанс, которого нет нигде: **разрядка прописных**.
`--tr-label: 0.18em` и `--tr-mono: 0.32em` заданы одинаково для латиницы и
кириллицы. Кириллические прописные шире латинских и требуют **меньшей**
разрядки при том же оптическом результате. Для русских лейблов
`0.14–0.16em` вместо `0.18em`.

---

## РАЗДЕЛ 6. Текстура, палитра, движение — конкретные значения

Базовый отчёт покрывает палитры (§2.6, восемь наборов) и рецепт зерна (§3.1).
Здесь — только то, что применимо **к этому проекту**, с учётом уже принятых
токенов.

### 6.1 Палитра: что менять и что не трогать

**Не трогать.** Текущая система — тёплая нейтральная шкала `#060505 → #f7f4ec`
плюс одна акцентная температура `#c23a17 / #ef4a23 / #ff6a3d` — уже
соответствует всему, что базовый отчёт называет дорогим: near-black не чистый
чёрный (§2.3), один акцент (§2.6, правило 60/30/10), тёплый сдвиг вместо
нейтрального серого.

**Что добавить — вторую температуру для драматургии, не для декора.**
Сейчас на главной **одна** светлая секция (`ServicesSection`, `--n-950 #f7f4ec`,
скруглённая сверху на 40/50/60px). Это единственный температурный удар на
странице, и он стоит не в том месте — на услугах, самой скучной секции.

Рабочий набор из трёх температур для длинной страницы:

```
--temp-deep:  #060505   /* провал: полноэкранные паузы, перед ударом */
--temp-base:  #0c0b0a   /* дефолт страницы */
--temp-lift:  #141211   /* приподнято: секции-доказательства */
--temp-flip:  #f7f4ec   /* инверсия: ровно один раз за страницу */
```

Правило: `--temp-flip` использовать **один раз** и на самой сильной секции.
Второе появление обесценивает первое.

**Чего избегать (по §5 базового отчёта, всё подтверждено):** фиолетово-синие
градиенты, свечения цветной тенью на тёмном, стеклянные карточки на сетке,
градиентный текст. К этому проекту это отношения не имеет — палитра чистая.

### 6.2 Зерно — включить то, что уже написано

`.grain` (`tokens.css:237`) **не подключён ни на одном из пяти экранов услуг**.
На главной он есть (Hero, Process, Testimonials, FAQ, Contact), на экранах — нет.
Это единственная общая визуальная подпись сайта, и она наполовину выключена.

Текущие значения корректны и совпадают с каноническим рецептом базового отчёта §3.1:

```css
baseFrequency='0.82'  numOctaves='3'  stitchTiles='stitch'
opacity: 0.035        mix-blend-mode: overlay
```

Поправки к применению:

- `0.035` — нижняя граница рабочего диапазона. Базовый отчёт §3.1 фиксирует, что на **плоских тёмных поверхностях** продакшн-значения выше: `0.10–0.15`, потому что там зерно выполняет работу по устранению 8-битного бэндинга. Диапазон `0.02–0.08` — это для зерна **поверх изображений**. Рекомендация: держать `0.035` глобально, но поднимать до `0.09–0.12` на секциях с градиентной подложкой (Hero-канвас, радиальные свечения на `MotionScreen` и `Modeling3DScreen`).
- `numOctaves='3'` — правильный потолок, каждая октава примерно удваивает стоимость фильтра.
- **Никогда не анимировать** `baseFrequency` или `seed` — это форсирует перезапуск фильтра каждый кадр. Если нужен «живой» grain, базовый отчёт даёт единственно верный путь: предрендерить 3–5 кадров шума и переключать их CSS-анимацией `steps()`, ноль JS на кадр.
- `feTurbulence` — стоимость на **растеризации**, не на кадре. Статичное зерно бесплатно после отрисовки.

### 6.3 Хроматическая аберрация и дизеринг — дозировка

Базовый отчёт §3.6 покрывает аберрацию, §3.4 — halftone/dither. Практическое
правило для этого сайта:

- **Хроматическая аберрация** — только на движении и только на одном элементе за раз. RGB-сплит на статичном заголовке читается как пресет 2019 года. Уместное применение: сдвиг каналов на 0.5–1.5px в момент перехода между экранами (Раздел 4), затухающий к нулю за `--d-base` (400ms). В `MotionScreen` уже есть шейдер с simplex-шумом — аберрацию логично встроить туда как uniform, а не как CSS-фильтр.
- **Дизеринг / 1-bit** — сильный приём для `Modeling3DScreen`: показать превью модели дизерингом, а по наведению — чистый рендер. Это буквально «ремесло, которого не видно на финале» (Раздел 2.3). В остальных местах не использовать.
- **Галоид / полутон** — на этом сайте не нужен. Тёплая палитра + зерно уже дают материальность; полутон добавит ретро-читаемость, которая противоречит позиционированию.

### 6.4 Движение — что уже есть и чего не хватает

Система в `src/design/motion.ts` полная и корректная:

```
ease.standard   [0.32, 0.72, 0, 1]     — интерфейсный дефолт
ease.entrance   [0.16, 1, 0.3, 1]      — рабочая лошадь появлений (expo-out)
ease.exit       [0.7, 0, 0.84, 0]      — уходы
ease.editorial  [0.65, 0, 0.35, 1]     — шторки, крупные полотна
ease.soft       [0.4, 0, 0.2, 1]
ease.overshoot  [0.34, 1.56, 0.64, 1]

duration: instant .12 / fast .24 / base .4 / slow .65 / slower .95 / cinematic 1.4
spring:   cursor {520,40,0.6} / snappy {380,30,0.8} / gentle {180,26,1}
          heavy {90,22,1.4} / scroll {140,30,0.9, restDelta .0005}
stagger:  letter .018 / word .045 / line .075 / item .09 / block .14
inView:   { once: true, amount: 0.25, margin: '0px 0px -12% 0px' }
```

Сверка с таблицами базового отчёта §3a/§3b — расхождений нет, значения лежат
внутри рекомендованных диапазонов. `stagger.letter 0.018` точно попадает в
«15–20ms, достаточно быстро, чтобы читаться одним жестом». Ограничение
`SplitText` на `MAX_TOTAL = 0.6s` реализует правило бюджета стаггера
(`items × stagger + duration ≤ ~1.2s`) — это сделано грамотно.

**Три пробела:**

1. **Выходы не быстрее входов.** Базовый отчёт §3a: «exits should be ~30–40% shorter than entrances, because the user has already decided». Сейчас `ease.exit` определён, но отдельной шкалы длительностей для выходов нет. Добавить:
   ```ts
   duration.exitBase = 0.26   // против base 0.4
   duration.exitSlow = 0.42   // против slow 0.65
   ```
2. **`ease.overshoot [0.34, 1.56, 0.64, 1]` — опасный токен.** Базовый отчёт §4 (каталог AI-slop) прямо помечает «bounce and elastic easing» как признак сгенерированного интерфейса. Различие, которое там же проводится: пружина с честной массой/натяжением — нормально, декоративный overshoot, прикрученный к кнопке — тель. Рекомендация: `ease.overshoot` не использовать на главной вообще; оставить только внутри `MotionScreen`, где демонстрация кривых — это и есть контент.
3. **`inView.once: true` глобально.** Для длинной эмоциональной страницы это правильно на появлениях, но блокирует приёмы «дыхания» из Раздела 1.3 (секция, которая гаснет при уходе из кадра). Нужен второй пресет:
   ```ts
   inViewLive = { once: false, amount: 0.4 }
   ```

### 6.5 Lenis — текущая настройка

`src/hooks/useSmoothScroll.ts`: `lerp: 0.085`, `wheelMultiplier: 0.9`,
`touchMultiplier: 1.2`, `smoothWheel: true`; выключается при
`prefers-reduced-motion` и при `(pointer: coarse)`; нативный `scroll-behavior`
принудительно `auto`, чтобы не конфликтовать. Это соответствует
«minimum-harm configuration» из §6.3 базового отчёта — менять не нужно.

`lerp: 0.085` — заметно тяжелее дефолтного `0.1`. Для «дорогого» ощущения это
правильный сдвиг: вес скролла читается как вес сайта. Ниже `0.07` уходить не
стоит — начинается ощущение задержки ввода.

---

## РАЗДЕЛ 7. Метод и достоверность

**Как собиралось.** Три параллельных исследования по вебу плюс два аудита
кодовой базы проекта. Квота `WebSearch` в сессии была исчерпана (200/200),
поэтому веб-часть построена на **прямых обращениях (WebFetch) к
первоисточникам**: страницы словолитен, метаданные Google Fonts, MDN, caniuse,
документация Chrome и GSAP, HTML/CSS/JS-артефакты живых сайтов, теги Awwwards
«Technologies & Tools». Это дало высокую достоверность там, где источник
открылся, но лишило возможности искать материалы, URL которых не был известен
заранее.

**Что это значит для чтения отчёта:**

| Раздел | Достоверность |
|---|---|
| Аудит кода проекта (3, 3-БИС) | **Высокая** — прочитаны исходники. |
| Копирайт и приёмы недоступности (1.1, 1.1-БИС) | **Высокая** — все verbatim-строки извлечены из реального DOM/JSON-пейлоада. |
| Технологические стеки первых экранов (1.2) | **Высокая** там, где найдены прямые маркеры (`WebGLRenderer`, `three.module`, теги Awwwards). |
| Кириллическое покрытие (5.2-БИС, 5.3) | **Высокая** — проверено по метаданным Google Fonts и страницам конкретных гарнитур. Непроверенное помечено явно. |
| Статус View Transitions и scroll-driven (4.2) | **Высокая** — caniuse + MDN. |
| Посекционный ритм длинных страниц (1.3-БИС) | **Реконструкция** по разметке, порядку блоков и ассетам. WebFetch не исполняет JS, страницы не наблюдались отрендеренными. Не выдавать за проверенный факт. |
| «Что заезжено» в русской типографике (5.4) | **Профессиональное суждение**, не измеренные данные. |
| Числовые нормы движения (6.4, 4.2) | Смесь документированных значений (MDN, GSAP, Material) и ремесленной нормы. Помечено по месту. |

**Чего подтвердить не удалось — перечислено явно, чтобы не было принято за
факт:**

1. **Кириллица у Klim** (Söhne, Tiempos, Domaine, Founders Grotesk) — страницы не раскрывают языковую поддержку. Не закладываться.
2. **Кириллица у ABC Dinamo**, включая ABC Diatype — не подтверждена.
3. **Кириллица у Letters from Sweden** — не документирована ни у одного шрифта.
4. **CSTM Fonts** — `cstm.cc` не резолвится, `cstmfonts.com` отдаёт 401.
5. **PT Root UI, Nyghtserif, e-Ukraine, Steinbeck** — не проверены. **Yandex Sans** — свободной лицензии, судя по всему, нет; без юридической проверки не использовать.
6. **JetBrains Mono и Geist Mono** — кириллица не проверена (проверен только Geist Sans, кириллица есть).
7. **Интерактивные easing-кривые как UI-виджет, sound-synced секвенции, cursor-as-timeline-scrubber** на сайтах моушн-студий — верифицировать не удалось (JS не рендерился). Приёмы в Разделе 2.1 предложены по аналогии, а не по наблюдению.
8. **Before/after wipe** — классика VFX-брейкдаунов, но живого примера 2025–2026 на студийном сайте не зафиксировано.
9. **Реальные award-сайты с curtain-переходами** — не собраны, требовали поиска.
10. Недоступны в сессии: `thefwa.com` (HTTP 500), `godly.website` → `recent.design` (403), `land-book.com` (403). Мёртвые/переехавшие домены: `ashthorp.com` → `altcinc.com`, `panoply.tv` → `panoply.co.uk`, `koto.studio` → `koto.com`; не резолвятся `vucko.tv`, `gordonnl.com`, `robertbeier.com`.

**Наиболее ценный источник, обнаруженный в ходе работы:**
[Codrops](https://tympanus.net/codrops/) — жанр студийных технических
post-mortem там свежий (окт 2025 → июнь 2026) и содержит тот уровень
детализации, который невозможно получить фетчем самих SPA-сайтов.

---

## РАЗДЕЛ 8. Порядок внедрения

По убыванию отношения «эффект / трудозатраты».

**Сначала — вычитание (день работы, максимальный эффект):**
1. Убрать плашку `Открыт для проектов` и `CONTACT.availableFrom`.
2. Удалить `TestimonialsSection`.
3. Свернуть `StatsSection` в одну строку прозы.
4. Заменить Instrument Serif в `BrandingScreen` — у него нет кириллицы.
5. Убрать `FloatingGlyphs` и второй `CodeMarquee` из `DevelopmentScreen`.

**Затем — перестановка (полдня, меняет жанр сайта):**
6. Переставить `Projects` на позицию №2, сразу после Marquee.
7. Перенести единственную световую инверсию с `Services` на `Projects`.
8. Добавить две полноэкранные паузы `100svh` на `#060505`.

**Затем — переход между экранами (день, закрывает худшую точку сайта):**
9. Шторка на переиспользованном `LoadingScreen`: 340ms вход `ease.exit` → смена состояния → 620ms уход `ease.editorial`.
10. `layoutId` на пяти строках услуг для морфинга в заголовок экрана.

**Затем — по одному приёму на каждый слабый экран:**
11. `MotionScreen` — глобальная кривая из `EasingLab`.
12. `DevelopmentScreen` — измеренная панель вместо прозы + названный компромисс (Lenis vs нативный скролл).
13. `Modeling3DScreen` — док стадий пайплайна + живые метрики модели из GLTF.

**Последним — типографика и текстура:**
14. Подключить `.grain` на всех пяти экранах.
15. Мono-начертание для метаданных на главной (сейчас `.t-mono` — мёртвый CSS).
16. Кириллический трекинг прописных: `0.14–0.16em` вместо `0.18em`.
